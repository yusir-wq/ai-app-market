# AI应用广场 1.3 内页工具原型协作规范

本文档面向共同参与内页工具原型迭代的产品同学。目标不是记录讨论过程，也不是给前端写实现说明，而是统一后续工具内页的产品结构、设计约束、交互标准和验收方法，确保不同人做出来的内页像同一个产品，而不是多个页面拼在一起。

## 1. 第一性原则

AI应用广场面向的是 AI 小白用户，核心价值是“多模型、多工具、便宜好用”。内页工具原型的首要任务不是展示复杂能力，而是让用户一眼知道这个工具能解决什么问题、下一步该点哪里、花多少智点、结果在哪里看。

因此，所有内页工具都应遵循四个原则：

1. 信息少而准：每个区块只承担一个任务，不堆参数、不堆说明、不用专业术语教育用户。
2. 操作顺而稳：上传、选择、生成、查看结果、下载必须形成闭环，不能让用户猜下一步。
3. 视觉一致：框架、字体、颜色、圆角、阴影、图标、按钮和 hover 状态都沿用已确认规范。
4. 迭代可复用：先把 AI换背景打造成通用模板，再让扩图、写真、商品海报、风格转换、配色重绘等工具按同一结构复用和变体化。

## 2. 当前内页范围

当前已确认使用 AI换背景 作为图像工具内页 demo 模板。

- demo 页：`AI 换背景(demo页)`
- 访问路径：`#/image-tools/ai-background`
- 其他图像工具当前只展示一句提示：`使用AI换背景页面模板，此处不再展示效果。`
- 其他图像工具后续复用 AI换背景的页面结构，再根据具体工具替换参数、场景和结果信息。

当前图像工具菜单包含：

1. AI 扩图
2. AI 换背景(demo页)
3. AI 写真
4. AI 商品海报
5. AI 风格转换
6. AI 配色重绘

## 3. 页面框架不能动

内页工具不是独立新站，也不是营销页。每个内页必须嵌在 AI应用广场 1.3 的统一框架里。

必须保留：

1. 左侧品牌区、透明 logo、平台名称和 slogan。
2. 左侧折叠/展开按钮及自动折叠规则。
3. 左侧底部剩余智点、智点充值、邀请好友、账号入口。
4. 右侧白色画布和统一内容限宽。
5. 右下角悬浮功能入口。
6. 底部备案和版权信息。

不能擅自改变：

- 左侧导航宽度：展开 `252px`，折叠 `76px`。
- 右侧内容最大宽度：`1440px`。
- 右侧内容最小宽度：`1080px`。
- 右侧内容安全间距：`34px`。
- 左侧菜单 hover 是淡白，active 是纯白。
- 首页主菜单和内页工具菜单列表间距统一为 `4px`。
- 左侧普通菜单字号为 `14px`，字重 `400`；内页返回入口字号为 `12px`，视觉弱化。
- 左侧底部剩余智点文案和数值均为 `14px`；智点充值、邀请好友、账号 ID 均为 `14px`；邀请好友辅助文案为 `10px`。
- 页面最小验收宽度：`1400px`。

如果一个工具内容复杂，优先优化内部栅格和信息分层，不要突破统一框架。

## 4. 内页左侧菜单规范

进入 AI图像工具内页后，左侧主菜单会切换为图像工具内页菜单，但底部账号区等框架保留。

第一项是返回入口：

- 展开态文案：`AI图像工具`
- hover 提示：`返回主页，发现更多AI工具`
- 图标和文字弱化，不要做得像当前工具标题。
- 点击返回快速开始页。

工具菜单：

- 当前工具选中态必须持续可见。
- 选中态使用纯白背景，文字和图标为主色。
- hover 态使用淡白背景，不改变文字和图标颜色。
- 折叠态必须有白底 tooltip，不能黑底。

## 5. 工具内页信息架构

AI换背景当前模板结构如下，后续图像工具原则上优先复用。

1. 顶部工具条
2. 使用指南区
3. 操作区与风格/场景选择区
4. 生成结果
5. 生成历史

### 5.1 顶部工具条

顶部工具条用于告诉用户当前在哪个工具，并提供轻量工具操作。

- 左侧：工具图标 + 工具名称。
- 右侧：`收藏`、`分享`。
- 工具条是页面背景上的固定横条，不是突出的卡片。
- 底部分割线从左到右连贯。
- 收藏/分享为描边按钮，hover 不改变文字色，不加阴影。

收藏提示文案：

`按 ⌘D / Ctrl+D 将当前工具收藏到浏览器，后续使用更便捷`

### 5.2 使用指南区

使用指南区只解释工具能做什么，不做营销大标题。

当前 AI换背景文案规则：

- 标题：`使用指南`
- 标题不用蓝色，不加粗过度。
- 正文说明上传人物或商品图片后，选择背景风格，AI 保留主体并自然替换背景。
- 需要展示费用：`使用费用：490 智点/次（约 0.49 元）`

原则：

- 文案应让小白用户能秒懂。
- 不要堆“算法”“模型”“参数”等技术词。
- 不要把费用做得过于刺眼，但必须透明可见。

### 5.3 上传区

上传区是用户的第一步操作，必须显眼、稳定、可点。

上传前：

- 整个上传区域都可点击。
- 支持拖拽上传。
- 主文案：`点击或拖拽上传图片`
- 格式限制两行展示：
  - `支持 PNG、JPG、JPEG、WebP`
  - `单张不超过 10MB`
- 采用加号类图片图标。

上传后：

- 上传框尺寸不变。
- 只展示图片预览。
- 图片必须保持真实比例，不拉伸、不强行填满。
- 不展示文件名、大小、删除按钮。
- hover 时图片中间出现 `重新上传` 按钮。

上传提示：

`上传即表示您确认拥有图片及其中人物、商品、素材的合法授权，并承诺不用于违法违规用途。`

提示前需要有轻量提示图标。

### 5.4 生成设置

生成设置用于承载低理解成本的必要参数，不应做成复杂表单。

当前 AI换背景参数：

1. 图片比例
2. 背景色
3. 风格

参数展示规则：

- 模块标题：`生成设置`
- 每个参数是一整行高一点的下拉框样式。
- 左侧为参数名，右侧为当前值。
- 不使用浏览器默认 select。

图片比例：

- 当前默认：`接近原图`
- 下拉用比例图形 + 数字展示。
- 比例图形固定宽度占位，保证数字垂直左对齐。
- 不要把 `接近原图` 改成 `原图`。

背景色：

- 支持自动推荐和颜色选择。
- 自动推荐只保留一个选项，文案为 `自动推荐`。
- 自动推荐使用描边按钮样式，不使用背景色。
- hover 使用通用浅紫背景。
- 颜色按一级分组 + 二级色块展示。
- 色块 hover 展示颜色名称。
- 选中后展示颜色名称 + 色块。

风格：

- 风格不在下拉内选择，只从右侧风格区选择。
- 左侧参数自动同步选中的风格。
- 展示格式为：`分类/风格名称`，例如 `分类一/摄影棚`。
- 下拉时只提示：`请在右侧选择风格`。

生成按钮：

- 文案：`立即生成`
- 主按钮样式。
- hover 不透明度为 `80%`。
- 阴影要克制，不要泛光。
- 费用文案在按钮下居中弱化：`490 智点/次（约 0.49 元）`
- 不要写成 `使用费用：490 智点/次...`

### 5.5 选择风格区

选择风格区承担“给小白用户选择场景”的任务，应该丰富但不复杂。

标题：

- `选择风格`
- 字号 `18px`

分类 tab：

- 当前占位为：分类一、分类二、分类三、分类四、分类五、分类六。
- tab 样式沿用当前 AI换背景页，不新增色系。

风格卡片：

- 卡片只展示图片/色块和场景名称。
- 不展示描述、标签、按钮、图标等额外信息。
- 选中后同步左侧风格参数。

### 5.6 生成结果

生成结果未生成前不展示整块区域，避免页面显得空。

点击 `立即生成` 后：

- 页面自动滚动到生成结果位置。
- 结果区展示成功状态、预览图、结果参数和下载按钮。

标题：

- `生成结果`
- 字号 `18px`
- 样式与生成历史一致。

成功文案：

- 主文案：`生成成功！`
- 提示：`AI生成内容，仅供参考，请勿用于违法违规用途。`

结果参数：

- 风格
- 比例
- 尺寸
- 大小
- 格式

预览图：

- 使用固定背景容器。
- 预览图保持真实比例，不变形。
- 可用棋盘格表达透明/预览环境。
- 预览图需要两类水印：
  - 右下角 `AI生成`
  - 全局较弱的斜向网格/线条水印

预览提示：

`当前为带水印预览图，下载后可获取原图。右下角“AI生成”为合规标识，将随原图保留。`

下载按钮：

- 文案：`下载图片`
- 主按钮样式，与 `立即生成` 一致。
- 点击前必须弹确认窗口。

### 5.7 下载确认

所有下载入口都必须弹同一个确认窗口，包括生成结果下载和生成历史下载。

确认文案：

`下载即表示您确认对上传素材拥有合法使用权，知悉该图片为AI生成/编辑内容，并自行承担使用责任。`

操作：

- `取消`
- `确认并下载`

两个按钮同行展示，`确认并下载` 在右侧。下载确认属于强调确认弹窗：确认按钮宽度大于取消按钮，但高度与取消一致，按钮不加阴影。取消 hover 只改变背景，不改变文字颜色。

### 5.8 生成历史

标题：

- `生成历史`
- 字号 `18px`

标题右侧单行提示：

`历史记录将为您保留 3 天。为避免过期丢失，请及时下载到本地设备。`

历史项需要展示：

- 图片
- 风格
- 尺寸
- 大小
- 时间
- 下载
- 删除

时间规则：

- 今天：`今天 HH:mm`
- 非今天：`YYYY-MM-DD HH:mm`

历史操作规则：

- 下载在左，删除在右。
- 下载按钮样式与收藏/分享一致，点击同样弹下载确认。
- 删除按钮视觉必须比下载弱化，使用更淡的文字色和弱化描边/背景，不抢主操作权重。
- 删除用于移除当前历史记录，必须先弹出确认窗口，确认后才删除。删除确认属于普通确认弹窗：确认按钮与取消按钮宽度一致、高度一致，按钮不加阴影；后续接入真实接口时需要补充失败回滚和权限校验。

## 6. 视觉规范速查

### 6.1 色彩

- 主色：`#575ce9`
- 强调主色：`#4f55ec`
- 浅主色背景：`#f0f3ff`
- 页面底色：`#f8faff`
- 左侧导航底色：`#f7f9ff`
- 主标题：`#202434`
- 正文：`#3f4558`
- 辅助文字：`#8a91a6` / `#9097aa`
- 弱提示：`#b2b8c8`
- 常规线：`#e7ebf5`
- 弱线：`#f0f2f8`

### 6.2 字体和字重

- 不要随意加粗。
- 普通菜单、表单项、弹窗列表、辅助文案默认不加粗。
- 只有标题、关键数字、选中态、明确强调信息才提高字重。
- `选择风格`、`生成结果`、`生成历史` 当前标题字号为 `18px`。

### 6.3 阴影和边框

- 主面板使用轻阴影，参考 `0 8px 22px rgba(38, 44, 72, 0.026-0.028)`。
- 不使用大范围泛光阴影。
- hover 不使用大阴影。
- 卡片描边要淡，不要灰脏。

### 6.4 图标

- UI 图标使用 Phosphor Icons，当前原型使用 `@phosphor-icons/web` 的 regular 和 duotone。
- 不临时手绘 SVG，不自创抽象图标，不混用风格差异明显的图标库。
- 工具图标、按钮图标、提示图标要保持同一套 Phosphor 线性风格。
- 品牌 logo 使用透明 PNG：`assets/ai-logo-transparent-cropped.png`。
- 厂商、品牌、模型图标必须使用真实品牌图标或已确认本地资源，不允许用首字母、渐变块、随机图标替代真实品牌图标。
- 模型相关区域的图标规则要与快速开始页保持一致：厂商 tab 使用黑白品牌图标，模型列表和模型卡片使用厂商原色 logo。
- 同一厂商在搜索区、推荐模型、全部模型、后续模型内页中必须使用同一套 logo 资源和同一裁切比例，不能一个页面一种图标。
- 工具类图标和模型/厂商品牌图标不要混用：工具功能图标走 Phosphor，模型/厂商识别图标走真实品牌资源。

## 7. 交互规范速查

1. 所有可点击区域 hover 必须是鼠标手。
2. hover 默认不改变文字色和图标色，只改变轻背景、透明度、辅助动作显隐。
3. 左侧菜单规范独立：hover 淡白，active 纯白。
4. 弹窗、下拉、tooltip 需要有轻微延迟，避免误触。
5. 从触发区移动到浮层时不能立刻消失。
6. 本期只做视觉预留的点击事件，需要记录到待补任务。
7. 用户点击生成后，必须能看到反馈；结果在页面下方时要自动滚动。
8. 错误提示要放在用户看得到的位置，不要藏在按钮下面或页面底部。

## 8. 新工具页复用方法

后续新增或完善图像工具页时，请按这个顺序做：

1. 先确认工具用户任务：用户上传什么、选择什么、生成什么、下载什么。
2. 复用 AI换背景的页面骨架：顶部工具条、使用指南、上传/设置、右侧场景选择、结果、历史。
3. 只替换必要内容：工具名、使用说明、费用、上传限制、参数名称、场景分类、结果参数。
4. 不要新增大块营销区域，不要新增复杂参数面板。
5. 先补主路径，再补状态：未上传、上传后、参数下拉、选中风格、生成中、生成成功、生成失败、历史为空。
6. 如果一个工具确实不适合 AI换背景模板，先说明原因，再提出新布局方案，不要直接另起一套视觉。

## 9. 不要做的事

1. 不要改左侧框架。
2. 不要把工具内页做成独立 landing page。
3. 不要把普通 hover 做成文字变蓝、图标变蓝。
4. 不要滥用加粗。
5. 不要用大阴影和大渐变制造“高级感”。
6. 不要让上传图、结果图变形。
7. 不要把合规提示藏起来。
8. 不要删除已确认的下载确认。
9. 不要让其他工具页展示旧占位大标题和多余说明。
10. 不要把前端实现文档当产品原型规范直接照搬。

## 10. 对抗性检查清单

每次提交内页原型前，至少按以下清单自查。

### 10.1 框架检查

- 左侧品牌区、折叠按钮、底部账号区还在。
- 首页和内页左侧菜单状态没有互相污染。
- 展开和折叠态都能识别当前页面。
- `1400px` 宽度无横向滚动。
- 宽屏下内容没有无限拉宽，仍受 `1440px` 限制。

### 10.2 视觉检查

- 页面看起来是同一个产品，不像单独外包页面。
- 标题、正文、辅助文案层级清楚。
- 没有不必要的加粗。
- 没有大范围泛光阴影。
- 卡片描边不脏。
- 图标风格一致。
- 图片没有压扁、裁切异常、露黑边。

### 10.3 交互检查

- 所有按钮、卡片、tab、菜单项 hover 有鼠标手。
- hover 不误改普通文字和图标颜色。
- 下拉能打开，内容对齐，不遮挡关键区域。
- tooltip 和弹窗不会误触立刻出现，也不会移入时立刻消失。
- 生成按钮未满足条件时，错误提示在可见位置。
- 点击生成后能自动滚动到结果区域。
- 下载按钮弹出确认，不直接下载。

### 10.4 内容压力检查

- 智点数值用 `20,000,000` 不错位。
- 用户 ID 用 `chinaz_0000000` 不溢出。
- 手机号用 `199****9999` 不换行。
- 风格名较长时，`分类/风格名称` 不挤压下拉框。
- 生成历史提示保持单行，不换成两行。

### 10.5 合规检查

- 上传授权提示存在。
- 生成结果合规提示存在。
- 预览图说明存在。
- 右下角 `AI生成` 合规标识规则明确。
- 下载确认文案存在，且使用“您”。

## 11. 交付给研发前的说明格式

产品同学完成一个内页工具原型后，建议用下面格式同步给研发或前端 Codex：

```text
工具名称：
页面路径：
复用模板：
本次新增/调整模块：
主流程：
需要保留的视觉规范：
需要保留的交互状态：
本期只做视觉预留的事件：
待后端确认：
已做对抗性检查：
```

这样可以避免研发只看到页面截图，却不知道哪些是已确认规范、哪些只是占位。
