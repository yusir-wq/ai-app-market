# AI 应用广场 V1.2 产品功能结构图

> **版本：** V1.2  
> **生成日期：** 2026-06-16  
> **文档类型：** 产品功能结构图（开发交付物）  
> **目标读者：** 后端开发、前端开发、QA、AI Coding Agent

---

## 目录

- [1. 功能结构总图](#1-功能结构总图)
- [2. 模块分解](#2-模块分解)
- [3. 页面分解](#3-页面分解)
- [4. 业务流程](#4-业务流程)
- [5. 字段设计](#5-字段设计)
- [6. 状态设计](#6-状态设计)
- [7. 异常处理](#7-异常处理)
- [8. 权限设计](#8-权限设计)

---

## 1. 功能结构总图

```
AI 应用广场 V1.2
│
├── 🏠 首页工作台（Home）
│   │
│   ├── 品牌展示区
│   │   ├── Logo + 产品名称
│   │   └── 描述文案
│   │
│   ├── 模型快捷切换栏
│   │   └── 最近使用的 3 个模型（点击可 @提及）
│   │
│   ├── 对话输入区
│   │   ├── 输入框（支持 @模型名 触发提及弹窗）
│   │   ├── 已选模型 Pill 标签
│   │   ├── 📎 上传按钮
│   │   ├── @ 提及模型按钮
│   │   └── 发送按钮
│   │
│   └── 模型横向列表
│       ├── 聊天模型（横向滚动 + 查看全部）
│       ├── 图片模型（横向滚动 + 查看全部）
│       └── 视频模型（横向滚动 + 查看全部）
│
├── 💬 多模型对话（Chat）
│   │
│   ├── 用户消息气泡
│   │   ├── 消息内容
│   │   └── @提及的模型名称
│   │
│   ├── AI 回复卡片网格（1~4 列）
│   │   ├── 模型名称 + 徽章（联网/深度思考）
│   │   ├── 深度思考过程（默认折叠）
│   │   ├── 回复正文（Markdown/Code/Image/Video）
│   │   ├── 引用来源按钮
│   │   ├── 响应时间
│   │   └── 操作栏（复制/重新生成/回复）
│   │
│   ├── 引用来源抽屉（380px）
│   │   ├── 网站名称
│   │   ├── 标题
│   │   ├── 简介
│   │   └── 可点击 URL
│   │
│   └── 输入区
│       ├── 参考图上传区（必填/选填）
│       ├── 模型参数栏（聊天/图片/视频）
│       ├── @模型选择器
│       └── 发送按钮
│
├── 📂 对话历史管理
│   │
│   ├── 左侧导航历史列表
│   │   ├── 最近 10 条对话
│   │   ├── 标题 + 摘要 + 时间戳
│   │   └── 点击切换对话
│   │
│   ├── 历史记录抽屉
│   │   ├── 搜索历史
│   │   ├── 全部历史列表
│   │   ├── 重命名对话
│   │   └── 删除对话
│   │
│   └── 新建对话
│       └── 清空当前，返回首页
│
├── 🤖 模型分类页（Category）
│   │
│   ├── 聊天模型网格（3~4 列）
│   ├── 图片模型网格（3~4 列）
│   ├── 视频模型网格（3~4 列）
│   └── 点击模型卡片进入对话
│
├── 📋 模型详情页（Model Detail）
│   │
│   ├── 模型基本信息
│   │   ├── 名称 + Logo
│   │   ├── 描述
│   │   └── 能力标签
│   │
│   ├── 模型参数说明
│   │   ├── 支持的比例
│   │   ├── 质量选项
│   │   └── 特色功能
│   │
│   └── 快捷开始对话按钮
│
├── 🔌 MCP 服务中心
│   │
│   ├── MCP 选择器（工作台内）
│   │   ├── MCP 总开关
│   │   ├── 已添加 MCP 列表
│   │   ├── 启用/停用 MCP
│   │   └── 跳转 MCP 管理
│   │
│   ├── 我的 MCP（三列卡片）
│   │   ├── 服务名称
│   │   ├── 英文名称
│   │   ├── 状态 Badge
│   │   └── 配置/删除操作
│   │
│   ├── MCP 市场（三列卡片）
│   │   ├── 平台提供的 MCP 服务
│   │   ├── 搜索（名称/英文名/描述）
│   │   ├── 添加服务
│   │   └── 分页（上一页/下一页）
│   │
│   └── MCP 配置弹窗
│       ├── 服务名称
│       ├── 英文名称
│       ├── 介绍
│       ├── 类型
│       ├── URL
│       ├── 请求头
│       ├── 长运行模式
│       └── 超时设置
│
├── 💰 计费中心
│   │
│   ├── 消费记录
│   │   ├── 使用统计（总消耗/总调用/成功/失败）
│   │   ├── 消费明细列表
│   │   │   ├── 对话名称
│   │   │   ├── 模型名称
│   │   │   ├── 模型类型
│   │   │   ├── 状态
│   │   │   ├── Input Tokens
│   │   │   ├── Output Tokens
│   │   │   ├── 消耗金额
│   │   │   └── 时间范围
│   │   └── 图表可视化
│   │
│   └── 支付记录
│       ├── 充值历史列表
│       │   ├── 订单号
│       │   ├── 充值金额
│       │   ├── 支付方式
│       │   └── 支付时间
│       └── 分页
│
├── 🎁 邀请奖励系统
│   │
│   ├── 邀请弹窗
│   │   ├── 邀请规则说明
│   │   ├── 邀请链接展示
│   │   ├── 复制邀请链接
│   │   └── 分享按钮
│   │
│   └── 邀请记录列表
│       ├── 被邀请用户
│       ├── 注册时间
│       └── 奖励状态（待发放/已发放）
│
├── 🔐 用户认证
│   │
│   ├── 登录弹窗
│   │   ├── 手机号输入
│   │   ├── 验证码输入
│   │   └── 登录按钮
│   │
│   ├── 充值弹窗
│   │   ├── 充值金额选择（固定档位/自定义）
│   │   ├── 支付方式（支付宝/微信）
│   │   └── 确认支付
│   │
│   └── 个人中心
│       ├── 用户信息（头像/用户名）
│       ├── 智点余额
│       ├── 充值按钮
│       ├── 消费记录入口
│       ├── 支付记录入口
│       └── 邀请好友入口
│
└── 📱 移动端适配
    │
    ├── 顶部 Hamburger 菜单按钮
    ├── 左侧导航滑动抽屉
    ├── 半透明遮罩层
    ├── 多模型卡片强制 1 列
    ├── 输入框全宽
    └── 模型列表紧凑卡片
```

---

## 2. 模块分解

### 2.1 工作台模块（Workspace）

| 组件 | 职责 | 依赖 |
|------|------|------|
| `workspace.tsx` | 核心容器，管理所有全局状态 | AuthProvider, MCPProvider |
| `header.tsx` | 顶部导航，Logo、智点余额、充值、用户菜单 | AuthContext |
| `sidebar.tsx` / `nav-panel.tsx` | 左侧导航/模型列表 + 对话历史 | — |
| `workspace-content.tsx` | 内容区（欢迎页/对话列表/分类页） | — |
| `home-content.tsx` | 首页内容（品牌 + 模型快捷栏 + 输入框） | — |
| `input-area.tsx` / `home-input-area.tsx` | 输入区（底部输入框 + 参数栏） | — |
| `session-toolbar.tsx` | 会话工具栏（新建对话、历史记录） | — |
| `history-drawer.tsx` | 历史对话抽屉 | — |

### 2.2 聊天模块（Chat）

| 组件 | 职责 | 依赖 |
|------|------|------|
| `chat-messages.tsx` | 消息列表容器 | — |
| `user-message.tsx` | 用户消息渲染 | — |
| `ai-message.tsx` | AI 消息渲染 | — |
| `streaming-message.tsx` | 流式消息渲染 | — |
| `markdown-content.tsx` | Markdown 渲染 | — |
| `code-block.tsx` | 代码块高亮 | — |
| `image-preview-dialog.tsx` | 图片预览弹窗 | — |
| `video-preview-dialog.tsx` | 视频预览弹窗 | — |
| `video-card.tsx` | 视频卡片 | — |

### 2.3 认证模块（Auth）

| 组件 | 职责 | 依赖 |
|------|------|------|
| `login-modal.tsx` | 手机号+验证码登录弹窗 | AuthContext |
| `recharge-modal.tsx` | 充值弹窗 | AuthContext |

### 2.4 MCP 模块

| 组件 | 职责 | 依赖 |
|------|------|------|
| `mcp-center.tsx` | MCP 中心页面 | MCPContext |
| `mcp-service-list.tsx` | MCP 服务列表 | — |
| `mcp-service-card.tsx` | MCP 服务卡片 | — |

### 2.5 模型展示模块

| 组件 | 职责 | 依赖 |
|------|------|------|
| `model-card.tsx` | 模型卡片 | — |
| `model-list-row.tsx` | 模型列表行 | — |
| `model-response-card.tsx` | 模型回复卡片 | — |

### 2.6 计费模块

| 组件 | 职责 | 依赖 |
|------|------|------|
| `billing-usage.tsx` | 消费记录页面 | — |
| `billing-payments.tsx` | 支付记录页面 | — |

### 2.7 邀请模块

| 组件 | 职责 | 依赖 |
|------|------|------|
| `invite-dialog.tsx` | 邀请好友弹窗 | — |

---

## 3. 页面分解

### 3.1 首页（Home）

**页面路径：** `/`

**布局结构：**

```
┌─────────────────────────────────────────────────────────────┐
│  Header (固定顶部)                                           │
│  ├─ Logo + 产品名称                                          │
│  ├─ 剩余智点 + 充值按钮                                       │
│  └─ 用户头像下拉菜单 (消费记录/支付记录/退出)                   │
├──────────┬──────────────────────────────────────────────────┤
│          │                                                  │
│ Sidebar  │              HomeContent                         │
│ (可折叠)  │                                                  │
│          │  ┌──────────────────────────────────────────┐    │
│ ┌──────┐ │  │           品牌 Logo + 描述                 │    │
│ │Logo  │ │  └──────────────────────────────────────────┘    │
│ │新建  │ │  ┌──────────────────────────────────────────┐    │
│ │搜索  │ │  │         模型快捷切换栏 [Bot][Bot][Bot]      │    │
│ │历史  │ │  └──────────────────────────────────────────┘    │
│ │功能  │ │  ┌──────────────────────────────────────────┐    │
│ │用户  │ │  │           对话输入框                      │    │
│ └──────┘ │  │    ┌────────────────────────────────┐    │    │
│          │  │    │  📎  @  [发送]                  │    │    │
│          │  │    └────────────────────────────────┘    │    │
│          │  └──────────────────────────────────────────┘    │
│          │  ┌──────────────────────────────────────────┐    │
│          │  │ 聊天模型 [→] [Bot][Bot][Bot][Bot]         │    │
│          │  └──────────────────────────────────────────┘    │
│          │  ┌──────────────────────────────────────────┐    │
│          │  │ 图片模型 [→] [Bot][Bot][Bot][Bot]         │    │
│          │  └──────────────────────────────────────────┘    │
│          │  ┌──────────────────────────────────────────┐    │
│          │  │ 视频模型 [→] [Bot][Bot][Bot][Bot]         │    │
│          │  └──────────────────────────────────────────┘    │
│          │                                                  │
└──────────┴──────────────────────────────────────────────────┘
```

**功能点：**

| 功能 | 说明 |
|------|------|
| 品牌展示 | Logo + "AI应用广场" + 描述文案居中 |
| 模型快捷切换 | 最近使用的 3 个模型，点击可 @提及 |
| 对话输入 | 居中输入框，`max-w-3xl`，支持 `@模型名` |
| 操作按钮 | 📎 上传、@ 提及模型、发送 |
| 模型列表 | 3 行横向滚动列表（聊天/图片/视频），每行带"查看全部" |

---

### 3.2 对话页（Chat）

**页面路径：** `/`（viewMode = 'chat'）

**布局结构：**

```
┌─────────────────────────────────────────────────────────────┐
│  Header (固定顶部)                                           │
├──────────┬──────────────────────────────────────────────────┤
│          │  SessionToolbar (浮动)                            │
│ Sidebar  │  ├─ 新建对话                                       │
│ (可折叠)  │  └─ 打开历史记录                                   │
│          │                                                  │
│          │  ChatMessages (滚动内容区)                         │
│          │  ├─ UserMessage (右侧气泡)                         │
│          │  │   "帮我写一段Python代码"                         │
│          │  │   @DeepSeek @Claude                              │
│          │  │                                                  │
│          │  ├─ AIResponseGrid (1~4列)                        │
│          │  │   ┌──────────────┬──────────────┐               │
│          │  │   │ 🤖 DeepSeek  │ 🤖 Claude    │               │
│          │  │   │ 联网 ✓ 思考 ✓ │ 联网 ✓       │               │
│          │  │   │ [思考过程...] │              │               │
│          │  │   │ 回复内容...   │ 回复内容...   │               │
│          │  │   │ 引用来源(3)   │              │               │
│          │  │   │ ⏱ 2.4s [复制] │ ⏱ 1.8s [复制]│               │
│          │  │   └──────────────┴──────────────┘               │
│          │  │                                                  │
│          │  └─ ...更多消息                                    │
│          │                                                  │
│          │  InputArea (底部固定)                              │
│          │  ├─ 参考图缩略图条（可选）                          │
│          │  ├─ 模型参数栏（根据模型类型）                       │
│          │  ├─ 输入框 + @模型选择器                            │
│          │  └─ 发送按钮                                       │
└──────────┴──────────────────────────────────────────────────┘
```

**功能点：**

| 功能 | 说明 |
|------|------|
| 用户消息 | 右侧气泡，显示 @提及的模型名称 |
| AI 回复卡片 | 1~4 列网格，显示模型名称、徽章、思考过程、正文、引用、操作 |
| 引用来源抽屉 | 380px 宽，从右侧滑出，显示搜索结果列表 |
| 深度思考面板 | 默认折叠，点击展开显示推理过程 |
| 输入区 | 支持参考图上传、模型参数、@模型选择 |

---

### 3.3 分类页（Category）

**页面路径：** `/`（viewMode = 'category'）

**布局结构：**

```
┌─────────────────────────────────────────────────────────────┐
│  Header (固定顶部)                                           │
├──────────┬──────────────────────────────────────────────────┤
│          │                                                  │
│ Sidebar  │  CategoryContent                                 │
│ (可折叠)  │                                                  │
│          │  ┌────┬────┬────┐                                │
│          │  │Bot │Bot │Bot │  ← 3~4 列网格                  │
│          │  ├────┼────┼────┤                                │
│          │  │Bot │Bot │Bot │                                │
│          │  └────┴────┴────┘                                │
│          │                                                  │
└──────────┴──────────────────────────────────────────────────┘
```

**功能点：**

| 功能 | 说明 |
|------|------|
| 模型网格 | 3~4 列卡片布局 |
| 分类筛选 | 聊天/图片/视频 |
| 点击模型 | 进入对话页 |

---

### 3.4 MCP 中心页

**页面路径：** `/`（viewMode = 'mcp-center'）

**布局结构：**

```
┌─────────────────────────────────────────────────────────────┐
│  Header (固定顶部)                                           │
├──────────┬──────────────────────────────────────────────────┤
│          │  ┌──────────────────────────────────────────┐    │
│ Sidebar  │  │ [返回工作台]    Tabs: [我的MCP][MCP市场]  [操作]│    │
│ (可折叠)  │  └──────────────────────────────────────────┘    │
│          │                                                  │
│          │  ┌────────┬────────┬────────┐                   │
│          │  │ MCP卡  │ MCP卡  │ MCP卡  │  ← 三列卡片        │
│          │  ├────────┼────────┼────────┤                   │
│          │  │ MCP卡  │ MCP卡  │ MCP卡  │                   │
│          │  └────────┴────────┴────────┘                   │
│          │                                                  │
│          │  [上一页] 1 2 3 ... [下一页]                      │
│          │                                                  │
└──────────┴──────────────────────────────────────────────────┘
```

**功能点：**

| 功能 | 说明 |
|------|------|
| 头部三栏 | 左侧"返回工作台" + 中间 Tabs + 右侧操作按钮 |
| Tabs | 我的 MCP / MCP 市场 |
| 卡片三列 | 服务名称、英文名称、描述、状态 Badge |
| 搜索 | 按名称/英文名/描述搜索 |
| 分页 | 中文显示（上一页/下一页） |
| 企业定制 | 渐变背景按钮，跳转定制页面 |

---

### 3.5 消费记录页

**页面路径：** `/`（viewMode = 'billing-usage'）

**布局结构：**

```
┌─────────────────────────────────────────────────────────────┐
│  Header (固定顶部)                                           │
├──────────┬──────────────────────────────────────────────────┤
│          │  UsageStats (统计卡片)                            │
│ Sidebar  │  ┌────────┬────────┬────────┬────────┐           │
│ (可折叠)  │  │总消耗  │总调用  │成功    │失败    │           │
│          │  └────────┴────────┴────────┴────────┘           │
│          │                                                  │
│          │  UsageChart (图表)                               │
│          │                                                  │
│          │  UsageTable (明细列表)                            │
│          │  ┌──────────────────────────────────────────┐    │
│          │  │ 对话名称|模型|类型|状态|Tokens|金额|时间   │    │
│          │  └──────────────────────────────────────────┘    │
│          │                                                  │
└──────────┴──────────────────────────────────────────────────┘
```

**功能点：**

| 功能 | 说明 |
|------|------|
| 统计卡片 | 总消耗、总调用、成功、失败 |
| 图表 | 消费趋势可视化 |
| 明细列表 | 对话名称、模型名称、类型、状态、Tokens、金额、时间 |

---

### 3.6 支付记录页

**页面路径：** `/`（viewMode = 'billing-payments'）

**布局结构：**

```
┌─────────────────────────────────────────────────────────────┐
│  Header (固定顶部)                                           │
├──────────┬──────────────────────────────────────────────────┤
│          │  PaymentsTable (充值历史)                         │
│ Sidebar  │  ┌──────────────────────────────────────────┐    │
│ (可折叠)  │  │ 订单号|金额|支付方式|支付时间             │    │
│          │  └──────────────────────────────────────────┘    │
│          │                                                  │
│          │  [上一页] 1 2 3 ... [下一页]                      │
│          │                                                  │
└──────────┴──────────────────────────────────────────────────┘
```

**功能点：**

| 功能 | 说明 |
|------|------|
| 充值列表 | 订单号、金额、支付方式、支付时间 |
| 分页 | 中文显示（上一页/下一页） |

---

### 3.7 邀请好友弹窗

**触发方式：** 左侧导航「邀请好友」/ 个人中心「邀请好友」

**布局结构：**

```
┌─────────────────────────────┐
│       邀请好友               │
│                             │
│  邀请好友注册，双方各获       │
│  500 智点奖励               │
│                             │
│  ┌─────────────────────┐    │
│  │ https://xxx/invite/abc│   │
│  └─────────────────────┘    │
│  [复制邀请链接]  [分享]      │
│                             │
│  ── 邀请记录 ───────────    │
│  用户    注册时间  奖励状态   │
│  user1   06-10    ✅ 已发放  │
│  user2   06-08    ✅ 已发放  │
└─────────────────────────────┘
```

**功能点：**

| 功能 | 说明 |
|------|------|
| 规则说明 | 邀请好友注册，双方各获 500 智点 |
| 邀请链接 | 展示链接，支持复制 |
| 分享 | 调用系统分享 |
| 邀请记录 | 被邀请用户、注册时间、奖励状态 |

---

### 3.8 登录弹窗

**触发方式：** 未登录时触发操作

**布局结构：**

```
┌─────────────────────────────┐
│        登录                 │
│                             │
│  手机号                     │
│  ┌─────────────────────┐    │
│  │ 138****8888         │    │
│  └─────────────────────┘    │
│                             │
│  验证码                     │
│  ┌─────────────────────┐    │
│  │ 123456  [获取验证码]  │    │
│  └─────────────────────┘    │
│                             │
│  [登录]                     │
└─────────────────────────────┘
```

**功能点：**

| 功能 | 说明 |
|------|------|
| 手机号输入 | 11 位手机号格式校验 |
| 验证码输入 | 6 位数字验证码 |
| 获取验证码 | 60 秒倒计时 |
| 登录 | 验证成功后登录 |

---

### 3.9 充值弹窗

**触发方式：** 点击充值按钮

**布局结构：**

```
┌─────────────────────────────┐
│        充值                 │
│                             │
│  选择充值金额                │
│  [10元] [50元] [100元] [自定义]│
│                             │
│  支付方式                    │
│  [支付宝] [微信]             │
│                             │
│  支付成功即视为您同意《购买协议》│
│                             │
│  [确认支付]                 │
└─────────────────────────────┘
```

**功能点：**

| 功能 | 说明 |
|------|------|
| 金额选择 | 固定档位（10/50/100元）或自定义输入 |
| 支付方式 | 支付宝 / 微信支付 |
| 协议说明 | "支付成功即视为您同意《购买协议》" |
| 确认支付 | 模拟支付成功，余额更新 + Toast 提示 |

---

## 4. 业务流程

### 4.1 多模型对话流程

```
用户进入首页
    │
    ▼
在输入框输入消息
    │
    ▼
输入 @ 触发模型提及弹窗
    │
    ▼
选择 1~4 个模型（显示为 Pill 标签）
    │
    ▼
点击发送
    │
    ├─ 未登录 → 弹出登录窗口
    └─ 已登录 → 校验参考图（requiresReference=true 需上传）
              │
              ▼
        创建用户消息（右侧气泡，显示 @模型名）
              │
              ▼
        并行生成 AI 回复（每个模型独立卡片）
              │
              ├─ 加载态："思考中…" + 骨架屏
              └─ 完成：显示回复内容
              │
              ▼
        全部完成后输入框恢复可用
              │
              ▼
        用户可执行：复制 / 重新生成 / 回复（针对单个模型）
```

### 4.2 联网搜索展示流程

```
用户发送消息时启用联网搜索
    │
    ▼
AI 回复卡片生成
    │
    ▼
卡片底部显示 "引用来源 (N)"
    │
    ▼
用户点击 "引用来源"
    │
    ▼
右侧抽屉滑出（380px）
    │
    ▼
显示搜索结果列表
    │
    ├─ 网站名称
    ├─ 标题
    ├─ 简介
    └─ 可点击 URL（外链图标 ↗）
    │
    ▼
点击遮罩或关闭按钮 → 抽屉关闭
```

### 4.3 深度思考展示流程

```
用户发送消息时启用深度思考
    │
    ▼
AI 回复卡片生成
    │
    ▼
卡片正文前显示 "深度思考过程" 标题行（默认折叠）
    │
    ▼
用户点击展开
    │
    ▼
显示推理过程文本（等宽字体、浅色背景）
    │
    ▼
用户再次点击 → 折叠
```

### 4.4 邀请奖励流程

```
用户点击「邀请好友」
    │
    ▼
打开邀请弹窗
    │
    ▼
显示邀请链接
    │
    ├─ 点击「复制邀请链接」→ 复制到剪贴板 + Toast
    └─ 点击「分享」→ 调用系统分享
    │
    ▼
被邀请人通过链接注册
    │
    ▼
邀请人获得 500 智点
被邀请人获得 500 智点
    │
    ▼
邀请记录列表更新（状态：已发放）
```

### 4.5 MCP 服务配置流程

```
用户在工作台点击 MCP 选择器
    │
    ▼
展示已添加的 MCP 服务列表
    │
    ▼
用户启用/禁用 MCP 总开关
    │
    ├─ 禁用 → 所有 MCP 服务不可选，隐藏数量显示
    └─ 启用 → 可勾选具体 MCP 服务
              │
              ▼
        标题显示 "选择MCP服务（x）"
              │
              ▼
        点击 "MCP管理" → 跳转 MCP 服务页面
              │
              ▼
        在 MCP 市场浏览/搜索服务
              │
              ▼
        点击添加 → 弹出配置窗口
              │
              ▼
        按步骤配置 APIKey → 添加成功
              │
              ▼
        返回工作台，新 MCP 服务出现在选择器中
```

### 4.6 充值流程

```
用户点击 "充值" 按钮
    │
    ▼
弹出充值弹窗
    │
    ▼
选择充值金额（固定档位或自定义）
    │
    ▼
选择支付方式（支付宝/微信）
    │
    ▼
点击 "确认支付"
    │
    ▼
支付成功 → 余额更新 + Toast 提示
```

---

## 5. 字段设计

### 5.1 Conversation（对话）

| 字段名 | 类型 | 组件 | 默认值 | 必填 | 说明 |
|--------|------|------|--------|------|------|
| id | string | — | UUID | 是 | 对话唯一标识 |
| title | string | Text | "新对话" | 是 | 对话标题 |
| preview | string | Text | "" | 否 | 对话摘要预览 |
| createdAt | Date | — | new Date() | 是 | 创建时间 |
| messages | Message[] | — | [] | 是 | 消息列表 |

**示例数据：**

```
conv_001 | "Python代码求助" | "帮我写一段Python爬虫代码..." | 2026-06-15 14:30 | 5条消息
```

### 5.2 Message（消息）

| 字段名 | 类型 | 组件 | 默认值 | 必填 | 说明 |
|--------|------|------|--------|------|------|
| id | string | — | UUID | 是 | 消息唯一标识 |
| role | enum | — | 'user' | 是 | user / assistant |
| content | string | Textarea | "" | 是 | 消息内容 |
| contentType | enum | — | 'text' | 是 | text/markdown/code/image/video/mcp |
| timestamp | Date | — | new Date() | 是 | 发送时间 |
| modelId | string | — | null | 否 | assistant 消息关联的模型 ID |
| modelIds | string[] | — | [] | 否 | user 消息中 @提及的模型 ID 列表 |
| onlineSearch | boolean | Switch | false | 否 | 是否启用联网搜索 |
| deepThinking | boolean | Switch | false | 否 | 是否启用深度思考 |
| thinkingContent | string | Collapse | "" | 否 | 深度思考过程文本 |
| searchResults | SearchResult[] | Drawer | [] | 否 | 联网搜索结果 |
| responseTime | number | — | 0 | 否 | 响应时间（秒） |

**示例数据：**

```
msg_001 | user | "帮我写一段Python爬虫代码" | text | 2026-06-15 14:30 | null | ["deepseek", "claude"] | false | false | "" | [] | 0
msg_002 | assistant | "import requests..." | code | 2026-06-15 14:31 | "deepseek" | null | true | true | "首先分析需求..." | [3条结果] | 2.4
```

### 5.3 Model（模型）

| 字段名 | 类型 | 组件 | 默认值 | 必填 | 说明 |
|--------|------|------|--------|------|------|
| id | string | — | — | 是 | 模型唯一标识 |
| name | string | Text | — | 是 | 模型名称 |
| type | enum | Select | 'chat' | 是 | chat/image/video |
| description | string | Textarea | "" | 否 | 模型描述 |
| logo | string | Image | "" | 否 | 模型 Logo（emoji） |
| addedAt | Date | — | new Date() | 是 | 添加时间 |
| requiresReference | boolean | Switch | false | 否 | 是否必须上传参考图 |
| costPoints | number | Input | 0 | 否 | 单次生成消耗智点 |
| supportedReferences | string[] | Select | [] | 否 | content/style/character |

**示例数据：**

```
deepseek-v4 | DeepSeek V4 Pro | chat | "强大的多轮对话模型" | 🤖 | 2026-06-01 | false | 0 | []
gpt-image-2 | GPT-Image-2 | image | "高质量文生图" | 🎨 | 2026-06-01 | false | 100 | ["content", "style"]
qwen-image-max | Qwen-Image-Max | image | "中文场景理解" | 🖼️ | 2026-06-01 | true | 150 | ["content", "style", "character"]
gemini-3-flash | Gemini 3 Flash | video | "视频生成" | 🎬 | 2026-06-01 | false | 500 | []
```

### 5.4 SearchResult（联网搜索结果）

| 字段名 | 类型 | 组件 | 默认值 | 必填 | 说明 |
|--------|------|------|--------|------|------|
| id | string | — | UUID | 是 | 结果唯一标识 |
| siteName | string | Text | "" | 是 | 网站名称 |
| title | string | Text | "" | 是 | 标题 |
| snippet | string | Textarea | "" | 是 | 简介 |
| url | string | Link | "" | 是 | 链接地址 |

**示例数据：**

```
sr_001 | "知乎" | "Python爬虫入门教程" | "本文介绍如何使用Python的requests库..." | "https://zhuanlan.zhihu.com/p/xxx"
sr_002 | "CSDN" | "Python爬虫实战案例" | "分享10个实用的Python爬虫项目..." | "https://blog.csdn.net/xxx"
```

### 5.5 InviteRecord（邀请记录）

| 字段名 | 类型 | 组件 | 默认值 | 必填 | 说明 |
|--------|------|------|--------|------|------|
| id | string | — | UUID | 是 | 记录唯一标识 |
| invitedUserId | string | — | "" | 是 | 被邀请用户 ID |
| invitedUserName | string | Text | "" | 是 | 被邀请用户名称 |
| registeredAt | Date | — | new Date() | 是 | 注册时间 |
| rewardStatus | enum | Badge | 'pending' | 是 | pending / granted |
| rewardPoints | number | — | 500 | 是 | 奖励智点数 |

**示例数据：**

```
inv_001 | user_002 | "张三" | 2026-06-10 10:00 | granted | 500
inv_002 | user_003 | "李四" | 2026-06-08 15:30 | granted | 500
```

### 5.6 User（用户）

| 字段名 | 类型 | 组件 | 默认值 | 必填 | 说明 |
|--------|------|------|--------|------|------|
| id | string | — | UUID | 是 | 用户唯一标识 |
| phone | string | Input | "" | 是 | 手机号 |
| balance | number | — | 0 | 是 | 余额（元） |
| avatar | string | Image | "" | 否 | 头像 URL |

**示例数据：**

```
user_001 | 138****8888 | 150.00 | "https://avatar.example.com/xxx"
```

### 5.7 UsageRecord（消费记录）

| 字段名 | 类型 | 组件 | 默认值 | 必填 | 说明 |
|--------|------|------|--------|------|------|
| id | string | — | UUID | 是 | 记录唯一标识 |
| conversationName | string | Text | "" | 是 | 对话名称 |
| modelName | string | Text | "" | 是 | 模型名称 |
| modelType | enum | Select | 'chat' | 是 | chat/image/video |
| status | enum | Badge | 'success' | 是 | success/failed/generating |
| inputTokens | number | — | 0 | 是 | Input Tokens |
| outputTokens | number | — | 0 | 是 | Output Tokens |
| cost | number | — | 0 | 是 | 消耗金额 |
| startTime | string | — | "" | 是 | 开始时间 |
| endTime | string | — | "" | 是 | 结束时间 |

**示例数据：**

```
usage_001 | "Python代码求助" | "DeepSeek V4 Pro" | chat | success | 150 | 320 | 0.05 | 2026-06-15 14:30 | 2026-06-15 14:31
```

### 5.8 PaymentRecord（支付记录）

| 字段名 | 类型 | 组件 | 默认值 | 必填 | 说明 |
|--------|------|------|--------|------|------|
| id | string | — | UUID | 是 | 记录唯一标识 |
| orderNo | string | Text | "" | 是 | 订单号 |
| amount | number | — | 0 | 是 | 充值金额 |
| payTime | string | — | "" | 是 | 支付时间 |
| payMethod | enum | Select | 'alipay' | 是 | alipay/wechat |

**示例数据：**

```
pay_001 | "ORD202606150001" | 50.00 | 2026-06-15 10:00 | alipay
```

---

## 6. 状态设计

### 6.1 对话状态（Conversation）

```
[新建] → [进行中] → [已完成]
            │
            ↓
         [已归档]
            │
            ↓
         [已删除]
```

| 状态 | 说明 |
|------|------|
| 新建 | 对话刚创建，无消息 |
| 进行中 | 有消息交互 |
| 已完成 | 对话结束 |
| 已归档 | 用户手动归档 |
| 已删除 | 用户删除，软删除 |

### 6.2 消息状态（Message）

```
[发送中] → [成功]
    │
    ↓
[失败] → [重试]
```

| 状态 | 说明 |
|------|------|
| 发送中 | 消息正在发送/生成中 |
| 成功 | 消息发送/生成成功 |
| 失败 | 发送/生成失败，可重试 |

### 6.3 AI 回复状态（Assistant Message）

```
[思考中] → [生成中] → [成功]
    │           │
    ↓           ↓
[失败]      [失败]
```

| 状态 | 说明 |
|------|------|
| 思考中 | 模型正在思考（显示骨架屏） |
| 生成中 | 模型正在生成回复内容 |
| 成功 | 回复生成完成 |
| 失败 | 生成失败，显示错误信息 |

### 6.4 邀请奖励状态（InviteRecord）

```
[待发放] → [已发放]
```

| 状态 | 说明 |
|------|------|
| 待发放 | 被邀请人已注册，奖励待发放 |
| 已发放 | 奖励已发放到双方账户 |

### 6.5 MCP 服务状态（MCPService）

```
[已启用] ↔ [已停用]
```

| 状态 | 说明 |
|------|------|
| 已启用 | MCP 服务可用，可被勾选 |
| 已停用 | MCP 服务不可用，不可勾选 |

### 6.6 消费记录状态（UsageRecord）

```
[生成中] → [成功]
    │
    ↓
[失败]
```

| 状态 | 说明 |
|------|------|
| 生成中 | 内容正在生成 |
| 成功 | 生成成功，已扣费 |
| 失败 | 生成失败，未扣费/退费 |

---

## 7. 异常处理

### 7.1 全局异常

| 异常场景 | 处理方式 | 提示文案 |
|----------|----------|----------|
| 网络错误 | Toast 提示 | "网络错误，请稍后重试" |
| 系统错误 | Toast 提示 | "系统繁忙，请稍后重试" |
| 未登录 | 弹出登录弹窗 | — |
| 权限不足 | Toast 提示 | "您没有权限执行此操作" |
| 数据加载失败 | 空状态 + 重试按钮 | "数据加载失败，点击重试" |

### 7.2 对话异常

| 异常场景 | 处理方式 | 提示文案 |
|----------|----------|----------|
| 模型回复失败 | 卡片内显示错误 | "生成失败，点击重新生成" |
| 输入内容为空 | 发送按钮 disabled | — |
| 超过模型数量限制 | Toast 提示 | "最多同时选择 4 个模型" |
| 必填参考图未上传 | 发送按钮 disabled + Tooltip | "请上传参考图" |
| 余额不足 | Toast 提示 | "智点不足，请充值" |
| 输入框 @模型不存在 | 过滤不存在的模型 | — |

### 7.3 上传异常

| 异常场景 | 处理方式 | 提示文案 |
|----------|----------|----------|
| 文件格式不支持 | Toast 提示 | "仅支持 .jpg, .jpeg, .png, .webp 格式" |
| 文件大小超限 | Toast 提示 | "单张图片大小不能超过 5MB" |
| 上传数量超限 | Toast 提示 | "最多上传 9 张图片" |
| 上传失败 | Toast 提示 | "上传失败，请重试" |

### 7.4 登录异常

| 异常场景 | 处理方式 | 提示文案 |
|----------|----------|----------|
| 手机号格式错误 | 输入框校验 | "请输入正确的手机号" |
| 验证码错误 | Toast 提示 | "验证码错误，请重新输入" |
| 验证码过期 | Toast 提示 | "验证码已过期，请重新获取" |
| 发送验证码频率过高 | 按钮冷却 | "请 60 秒后重试" |

### 7.5 充值异常

| 异常场景 | 处理方式 | 提示文案 |
|----------|----------|----------|
| 金额输入非法 | 输入框校验 | "请输入有效的金额" |
| 支付失败 | Toast 提示 | "支付失败，请重试" |
| 支付超时 | Toast 提示 | "支付超时，请重新发起" |

### 7.6 MCP 异常

| 异常场景 | 处理方式 | 提示文案 |
|----------|----------|----------|
| MCP 配置错误 | Toast 提示 | "配置参数错误，请检查" |
| MCP 服务不可用 | Badge 标识 | 显示"服务异常" |
| APIKey 无效 | Toast 提示 | "APIKey 无效，请重新配置" |

### 7.7 邀请异常

| 异常场景 | 处理方式 | 提示文案 |
|----------|----------|----------|
| 复制链接失败 | Toast 提示 | "复制失败，请手动复制" |
| 邀请链接失效 | Toast 提示 | "邀请链接已失效" |
| 重复邀请 | Toast 提示 | "该用户已被邀请" |

---

## 8. 权限设计

### 8.1 角色定义

| 角色 | 说明 |
|------|------|
| 访客（Visitor） | 未登录用户，可浏览首页和模型列表 |
| 普通用户（User） | 已登录用户，可使用全部功能 |

### 8.2 功能权限矩阵

| 功能 | 访客 | 普通用户 |
|------|------|----------|
| 浏览首页 | ✅ | ✅ |
| 浏览模型列表 | ✅ | ✅ |
| 查看模型详情 | ✅ | ✅ |
| 发送消息 | ❌ | ✅ |
| 查看对话历史 | ❌ | ✅ |
| 查看消费记录 | ❌ | ✅ |
| 查看支付记录 | ❌ | ✅ |
| 充值 | ❌ | ✅ |
| 邀请好友 | ❌ | ✅ |
| MCP 服务管理 | ❌ | ✅ |
| 个人中心 | ❌ | ✅ |

### 8.3 数据权限

| 数据类型 | 访客 | 普通用户 |
|----------|------|----------|
| 模型数据 | 只读 | 只读 |
| 对话数据 | 无权限 | 仅自己的对话 |
| 消息数据 | 无权限 | 仅自己对话的消息 |
| 消费记录 | 无权限 | 仅自己的记录 |
| 支付记录 | 无权限 | 仅自己的记录 |
| 邀请记录 | 无权限 | 仅自己的记录 |
| MCP 配置 | 无权限 | 仅自己的配置 |
| 用户数据 | 无权限 | 仅自己的数据 |

---

## 附录

### A. 组件清单

```
components/
├── workspace/
│   ├── workspace.tsx           # 核心容器
│   ├── header.tsx              # 顶部导航
│   ├── sidebar.tsx             # 侧边栏
│   ├── nav-panel.tsx           # 导航面板
│   ├── workspace-content.tsx   # 内容区
│   ├── home-content.tsx        # 首页内容
│   ├── input-area.tsx          # 输入区
│   ├── home-input-area.tsx     # 首页输入区
│   ├── session-toolbar.tsx     # 会话工具栏
│   ├── history-drawer.tsx      # 历史抽屉
│   ├── chat-history-item.tsx   # 历史条目
│   ├── model-card.tsx          # 模型卡片
│   ├── model-list-row.tsx      # 模型列表行
│   ├── model-response-card.tsx # 模型回复卡片
│   ├── billing-usage.tsx       # 消费记录
│   ├── billing-payments.tsx    # 支付记录
│   ├── mcp-center.tsx          # MCP 中心
│   ├── mcp-service-list.tsx    # MCP 服务列表
│   ├── mcp-service-card.tsx    # MCP 服务卡片
│   └── invite-dialog.tsx       # 邀请弹窗
│
├── chat/
│   ├── chat-messages.tsx       # 消息列表容器
│   ├── user-message.tsx        # 用户消息
│   ├── ai-message.tsx          # AI 消息
│   ├── streaming-message.tsx   # 流式消息
│   ├── markdown-content.tsx    # Markdown 渲染
│   ├── code-block.tsx          # 代码块
│   ├── image-preview-dialog.tsx# 图片预览
│   ├── video-preview-dialog.tsx# 视频预览
│   └── video-card.tsx          # 视频卡片
│
├── auth/
│   ├── login-modal.tsx         # 登录弹窗
│   └── recharge-modal.tsx      # 充值弹窗
│
├── ui/                         # 40+ shadcn/ui 组件
│   ├── button.tsx
│   ├── dialog.tsx
│   ├── dropdown-menu.tsx
│   ├── tabs.tsx
│   ├── input.tsx
│   ├── select.tsx
│   ├── tooltip.tsx
│   ├── avatar.tsx
│   ├── badge.tsx
│   ├── card.tsx
│   ├── scroll-area.tsx
│   ├── sheet.tsx
│   ├── switch.tsx
│   └── sonner.tsx              # Toast
│
└── theme-provider.tsx          # 主题提供者
```

### B. 数据流图

```
用户操作
    │
    ▼
Workspace (状态中心)
    │
    ├─ 模型选择 ──→ Sidebar / WorkspaceContent / InputArea
    ├─ 消息状态 ──→ WorkspaceContent / ChatMessages
    ├─ 输入状态 ──→ InputArea
    ├─ 历史记录 ──→ HistoryDrawer
    ├─ 页面导航 ──→ Header / BillingUsage / BillingPayments
    ├─ MCP 状态 ──→ MCPProvider → MCP 选择器 / MCP 中心
    └─ 登录状态 ──→ AuthContext → 所有组件
```

### C. 相关文档

| 文档 | 路径 |
|------|------|
| V1.2 产品迭代文档 | `AI应用广场V1.2产品迭代文档.md` |
| V1.2 设计文档 | `docs/specs/2026-06-11-v1.2-design.md` |
| V1.1 产品文档 | `AI应用广场V1.1产品文档.md` |
| V1.2 项目上下文 | `V1.2_PROJECT_CONTEXT.md` |
