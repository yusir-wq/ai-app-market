# 任务简报：AI 语音转文字 页面重构

> 基于 V1.3 设计规范 `inner-tool-prototype-pm-guide.md` + `V1.3重构-完整设计规范汇总.md`  当前状态：开发阶段，持续迭代中

---

## 一、设计规范摘要

### 1.1 全局框架

| 项 | 规范值 |
|--|------|
| 右侧内容最大宽度 | `1440px` |
| 右侧内容最小宽度 | `1080px` |
| 右侧内容安全间距 | `34px`（左右） |
| 页面最小验收宽度 | `1400px` |
| 页面底色 | `#FFFFFF` |
| 主色（按钮/图标） | `#4f55ec` |
| 文字主色 | `#202434` |
| 文字次色 | `#3f4558` |
| 文字弱色 | `#8a91a6` |
| 卡片边框 | `#f0f2f8` |
| 内部分隔线 | `#e7ebf5` |
| 卡片背景（浅色） | `#f8faff` |
| 卡片阴影 | `0 8px 22px rgba(38,44,72,0.028)` |
| 圆角（大卡片） | `12px` |
| 圆角（按钮/小元素） | `10px` / `6px` |

### 1.2 字体规范

| 层级 | 字号 | 字重 | 颜色 |
|------|------|------|------|
| 卡片标题（h3） | `text-base` | `font-medium` | `#202434` |
| 正文 | `text-sm` | `font-normal` | `#3f4558` / `#8a91a6` |
| 辅助提示 | `text-xs` / `text-[11px]` | `font-normal` | `#8a91a6` |
| 按钮文字 | `text-sm` / `text-xs` | `font-medium` | 视按钮而定 |

**原则**：不随意加粗，标题、关键数字、选中态才提高字重。不使用蓝色作为标题色。

### 1.3 按钮规范

- 主按钮：`bg-[#4f55ec] hover:bg-[#4f55ec]/80`，文字白色，无 shadow
- 描边按钮（收藏/分享）：`border-border` + 无 shadow，hover 不改文字色
- 操作按钮（编辑/复制/下载）：`variant="ghost"`，文字弱化色，hover 变前景色
- 圆角统一：`rounded-[10px]`

### 1.4 弹窗规范

- 确认弹窗：标题 `16px` 居中，正文 `12px` 居中，按钮等宽等高，不加 shadow，点击空白不关闭
- 成功/失败提示：toast，不弹窗确认

---

## 二、重构目标

重构 `AI 语音转文字` 工具内页，使其成为 V1.3 的**模板参考页**，后续音频类工具（视频转文字、文本转语音等）复用此结构。

### 2.1 页面信息架构（已确认）

```
Header（64px 横条：返回+标题+收藏+分享）
├── Tabs（场景介绍 | 使用应用）— 滑动按钮样式，居中
│
├── 场景介绍 Tab
│   ├── 标题区（Hero）
│   ├── 使用流程（3步：上传→转写→导出）
│   ├── 功能特性（6项卡片网格）
│   ├── 适用场景（6项彩色卡片）
│   └── 常见问题（FAQ 手风琴）
│
└── 使用应用 Tab
    ├── 使用指南卡片（230px 高，左文右图）
    │   └── 封面图占位：180px 高，宽度 ≈ 624px，待生成 3.56:1 横版插图
    ├── 上传卡片 + 生成设置卡片（等高，5:4 比例）
    │   ├── 上传区：拖拽上传/文件信息卡 + 合规提示
    │   └── 生成设置：语言+4个开关 + 立即生成按钮 + 费用
    ├── 生成结果卡片（渐变色头部，3:2 双栏）
    │   ├── 左侧：转写内容（600px 高可滚动）+ 编辑/复制/导出 + 音频播放器
    │   └── 右侧：智能总结（初始页/结果页）+ 对话输入框
    └── 生成历史卡片（渐变色头部，列表卡片式）
```

### 2.2 交互闭环

```
上传音视频 → 选择设置 → 点击生成 → 自动滚动到结果 → 转写内容 + 智能总结
                                                    ↓
                                            音频播放器（播放/暂停/进度/倍速）
                                                    ↓
                                            编辑/复制/导出 TXT / 重新生成总结
                                                    ↓
                                            历史记录保留 3 天 → 下载/删除（需确认）
```

---

## 三、关键约束（不可突破）

### 3.1 框架约束

- 左侧导航宽度：展开 `252px`，折叠 `76px`（不可改）
- 右侧内容 `max-w-[1440px] mx-auto px-[34px]`
- Header 全宽 `px-[34px]`，内容 `max-w-[1440px]` 只作用于内容区
- 页面最小宽度 `1400px`
- 底部备案和版权信息保留

### 3.2 视觉约束

- 所有卡片统一阴影：`0 8px 22px rgba(38,44,72,0.028)`
- 卡片边框：`border-[#f0f2f8]`
- 内部分隔线：`divide-[#e7ebf5]` / `border-[#e7ebf5]`
- 列表 hover 态：`hover:bg-[#f8faff]`
- 按钮圆角：`rounded-[10px]`（主按钮），`rounded-[6px]`（小标签）

### 3.3 上传区约束

- 不展示文件名、大小、删除按钮（上传后）
- hover 时显示「重新上传」遮罩
- 加号图标：Phosphor `PlusCircle`（非 Upload 箭头）
- 上传后合规提示必须保留

### 3.4 历史记录约束

- 删除必须先弹出确认弹窗（AlertDialog）
- 头部渐变色：`linear-gradient(180deg, #f8faff 0%, #ffffff 100%)`
- 字段双行显示：标题行 `text-[11px] text-muted-foreground` + 内容行 `text-sm`

### 3.5 生成结果约束

- 标题：「生成结果」（非「生成成功！」）
- 成功文案：转写内容头部可带「生成成功！」，但卡片标题保持「生成结果」
- 转写内容区高度：600px，超出滚动
- 智能总结：初始页（图标+主副文案+开始总结按钮）→ 结果页（重新生成/复制）
- 音频播放器：播放/暂停 24px、进度条可拖动、时间显示、倍速 0.5x~2.0x

---

## 四、文件结构

```
f:\v0 Wireframe\AI APP Market\components\agent\
├── agent-speech-to-text-experience.tsx   ← 主文件（使用应用 Tab）
│   ├── 顶部 Header（64px）
│   ├── Tabs（场景介绍 | 使用应用）
│   ├── 使用指南卡片（230px，左文右图）
│   ├── 上传卡片 + 生成设置卡片（5:4 等高）
│   ├── 生成结果卡片（3:2 双栏，含音频播放器+智能总结）
│   ├── 生成历史卡片（列表式）
│   └── 删除确认弹窗（AlertDialog）
│
├── agent-speech-to-text-intro.tsx        ← 场景介绍 Tab
│   ├── HeroSection（标题+描述）
│   ├── StepsSection（3步使用流程）
│   ├── FeaturesSection（6项功能特性卡片）
│   ├── ScenariosSection（6项适用场景卡片）
│   └── FAQSection（6项手风琴 FAQ）
│
├── agent-text-to-speech-experience.tsx    ← 其他音频工具（参考复用）
├── agent-text-to-speech-intro.tsx
│
f:\v0 Wireframe\AI APP Market\public\covers\
├── agent-speech-to-text.jpg              ← 封面图（待生成，1368×768 → 需裁切为 3.56:1）
│                                         建议生成尺寸：1280 × 360
```

### 4.1 依赖组件

```
@/components/ui/button          → 主按钮/ghost按钮/outline按钮
@/components/ui/switch          → 生成设置开关
@/components/ui/select          → 语言选择下拉
@/components/ui/alert-dialog     → 删除确认弹窗
@/lib/mock-data                  → Agent 类型定义
@/lib/utils                      → cn 工具
sonner                            → toast 提示
@phosphor-icons/react            → 所有图标（PlusCircle/Play/Pause/Robot 等）
```

---

## 五、当前已知问题（待修复）

| 优先级 | 问题 | 位置 |
|--------|------|------|
| 🔴 高 | 上传区加号图标与文字间距过远（约40px→需16px） | 上传卡片 |
| 🔴 高 | 场景介绍页面未按设计规范统一（卡片阴影/边框/字重） | agent-speech-to-text-intro.tsx |
| 🟡 中 | 生成历史字段宽度需精确计算（转写内容500px在1440px下的实际占比） | 历史卡片 |
| 🟡 中 | 封面图待生成（1280×360，3.56:1） | public/covers/ |
| 🟢 低 | 音频播放器进度条滑块 CSS 自定义样式 | 结果卡片 |

---

## 六、复用模板说明

后续音频类工具（视频转文字、文本转语音、视频配音等）按此结构复用：

1. 复制 `agent-speech-to-text-experience.tsx` + `agent-speech-to-text-intro.tsx`
2. 替换：
   - 标题「AI语音转文字」→ 新工具名称
   - 封面图路径
   - 使用指南描述文字
   - 生成设置参数（开关项、下拉项）
   - 结果区内容（转写→配音/字幕等）
   - 模拟数据（mockHistory、mockResult）
3. 保持：Header、Tabs、卡片布局、阴影、边框、字体规范不变

---

*生成时间：2026-07-09*
*规范来源：V1.3重构-完整设计规范汇总.md + inner-tool-prototype-pm-guide.md*
*当前文件版本：agent-speech-to-text-experience.tsx（已编译通过）*
