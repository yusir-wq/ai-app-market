AI应用广场运营后台（新菜单结构）
│
├── 🔧 工具
│   │
│   ├── 📋 AI模型
│   │   ├── 📊 KPI卡片
│   │   │   ├── 模型总数（计数）
│   │   │   ├── 已上架数（计数）
│   │   │   └── 未上架数（计数）
│   │   ├── 🔍 筛选区
│   │   │   ├── 模型名称（文本输入，模糊匹配）
│   │   │   ├── 状态（下拉：全部/已上架/未上架）
│   │   │   └── 计费方式（下拉：全部/固定扣费/倍率扣费/未开启计费）
│   │   ├── 📄 表格列
│   │   │   ├── 模型名称
│   │   │   ├── 前台分类（只读，如：对话模型）
│   │   │   ├── 计费方式（固定扣费/倍率扣费/未开启）
│   │   │   ├── 智点售价（数字，如：1智点/次 或 倍率2.0）
│   │   │   ├── 状态（已上架/未上架）
│   │   │   ├── 最近修改时间（时间戳）
│   │   │   └── 操作（编辑按钮）
│   │   ├── ⚙️ 操作交互
│   │   │   ├── 上架/下架（快捷按钮，直接切换状态，1步完成）
│   │   │   ├── 编辑（打开配置抽屉）
│   │   │   └── 查看调用日志（跳转运营数据>模型调用日志，带模型名称筛选）
│   │   └── 📝 编辑抽屉
│   │       ├── 基础信息
│   │       │   ├── 模型名称（只读）
│   │       │   ├── 前台分类（只读）
│   │       │   ├── 模型描述（文本域，前台展示给用户）
│   │       │   ├── 模型短描述（文本域，卡片展示用）
│   │       │   └── 上架状态（开关：上架/下架）
│   │       ├── 用户端展示
│   │       │   ├── 预设提示词（动态数组，最多4条，用户点击后自动填入对话）
│   │       │   └── 前台标签（动态数组，最多4条，模型卡片上展示）
│   │       └── 计费配置
│   │           ├── 是否开启计费（开关：开启/关闭）
│   │           ├── 预扣额度（数字输入，仅倍率扣费时显示，先冻结后多退少补）
│   │           ├── 扣费方式（单选）
│   │           │   ├── 固定扣费（不管上游成本，每次扣固定智点）
│   │           │   └── 倍率扣费（扣费=上游成本×倍率×1000，倍率即利润空间）
│   │           └── 每次扣费（数字输入，仅固定扣费时显示，如：1智点=0.001元）
│   │
│   ├── 📋 图片工具
│   │   ├── 📊 KPI卡片
│   │   │   ├── 工具总数（计数）
│   │   │   ├── 已上架数（计数）
│   │   │   ├── 今日调用数（计数）
│   │   │   └── 今日成本（金额，2位小数，单位：元）
│   │   ├── 🔍 筛选区
│   │   │   ├── 工具名称（文本输入，模糊匹配）
│   │   │   └── 状态（下拉：全部/已启用/已禁用）
│   │   ├── 📄 表格列
│   │   │   ├── 工具名称
│   │   │   ├── 显示顺序（数字，控制前台排序）
│   │   │   ├── 启用状态（已启用/已禁用）
│   │   │   ├── 创建时间
│   │   │   └── 操作（工具编辑、场景编辑、启用/禁用快捷按钮）
│   │   ├── ⚙️ 操作交互
│   │   │   ├── 新增图片工具（顶部按钮）
│   │   │   ├── 启用/禁用（快捷按钮，1步切换）
│   │   │   ├── 工具编辑（打开抽屉）
│   │   │   └── 场景编辑（打开抽屉）
│   │   └── 📝 编辑抽屉
│   │       ├── 工具信息
│   │       │   ├── 工具名称（文本输入，必填）
│   │       │   ├── 前端排序（数字输入，min=0）
│   │       │   ├── 是否需要结果审核（开关，可编辑）
│   │       │   ├── 是否要求用户上传主图（开关，禁用/只读）
│   │       │   └── 是否要求场景配置主提示词（开关，禁用/只读）
│   │       ├── 计费配置
│   │       │   ├── 计费方式（文本输入，禁用/只读，当前值：固定计费）
│   │       │   ├── 是否开启计费（开关，可编辑）
│   │       │   ├── 固定计费智点（数字输入，必填，min=0）
│   │       │   └── 预扣智点数（数字输入，必填，min=0）
│   │       └── 参数列表（支持多参数，底部"添加参数"按钮）
│   │           ├── 参数名称（文本输入，必填，如：比例）
│   │           ├── 参数类型（文本输入，禁用/只读，当前值：非大模型接口参数）
│   │           ├── 参数形式（文本输入，禁用/只读，当前值：单选项）
│   │           ├── 前端排序（数字输入，min=0）
│   │           ├── 开关配置（6个）
│   │           │   ├── 携带副提示词（开关）
│   │           │   ├── 必传（开关）
│   │           │   ├── 不允许场景修改（开关）
│   │           │   ├── 前端可见（开关）
│   │           │   ├── 允许前端修改（开关）
│   │           │   └── 非后处理参数（开关）
│   │           ├── 副提示词内容（文本域，可用%s占位，如：画面比例要求）
│   │           └── 选项内容（仅参数形式为"单选项"时显示）
│   │               ├── 应用预设选项（下拉选择）
│   │               ├── 添加选项（按钮，新增label/value行）
│   │               └── 选项列表（每行：label输入框 + value输入框 + 删除按钮，如：1:1 / 1:1）
│   │
│   ├── 📋 音视频工具
│   │   ├── 📊 KPI卡片
│   │   │   ├── 工具总数（计数）
│   │   │   ├── 已上架数（计数）
│   │   │   ├── 今日调用数（计数）
│   │   │   └── 今日成本（金额，2位小数，单位：元）
│   │   ├── 🔍 筛选区
│   │   │   ├── 工具名称（文本输入，模糊匹配）
│   │   │   └── 状态（下拉：全部/已启用/已禁用）
│   │   ├── 📄 表格列
│   │   │   ├── 工具名称
│   │   │   ├── 显示顺序（数字）
│   │   │   ├── 启用状态（已启用/已禁用）
│   │   │   ├── 创建时间
│   │   │   └── 操作（工具编辑、启用/禁用快捷按钮）
│   │   ├── ⚙️ 操作交互
│   │   │   ├── 新增音视频工具（顶部按钮）
│   │   │   ├── 启用/禁用（快捷按钮，1步切换）
│   │   │   └── 工具编辑（打开抽屉）
│   │   └── 📝 编辑抽屉（⚠️ 音视频工具页面当前无数据，以下基于历史记录，待在线核实）
│   │       ├── 工具信息
│   │       │   ├── 工具名称（文本输入）
│   │       │   ├── 前端排序（数字输入）
│   │       │   ├── 是否需要结果审核（开关）
│   │       │   └── 是否启用（开关）← 图片工具无此字段，靠列表快捷启用/禁用
│   │       ├── 计费配置
│   │       │   ├── 计费方式（只读，当前值：灵活计费）← 图片工具为"固定计费"
│   │       │   ├── 是否开启计费（开关）
│   │       │   ├── 预扣类型（下拉，默认"默认"）← 图片工具无此字段
│   │       │   └── 预扣智点（数字输入）
│   │       ├── 模型倍率配置 ← 图片工具无此区域
│   │       │   └── （字段待核实：倍率值、关联模型等）
│   │       └── 参数列表（待核实是否存在，与图片工具是否结构一致）
│   │
│   └── 📋 SEO配置
│       ├── 🔍 筛选区
│       │   ├── 工具名称（文本输入，模糊匹配）
│       │   └── 工具类型（下拉：全部/图片工具/音视频工具）
│       ├── 📄 表格列
│       │   ├── 工具名称
│       │   ├── 页面名称（如：AI风格转换）
│       │   ├── 页面访问路径（只读，如：/1）
│       │   ├── 页面标题（已配置/空）
│       │   ├── 状态（已配置/未配置）
│       │   └── 操作（编辑按钮）
│       ├── ⚙️ 操作交互：编辑（打开弹窗）
│       └── 📝 编辑弹窗
│           ├── 页面名称（只读）
│           ├── 页面访问路径（只读）
│           ├── 页面标题（文本输入，建议50-60字符，用于搜索结果标题）
│           ├── 页面关键词（文本输入，逗号分隔，用于搜索关键词）
│           └── 页面描述（文本域，建议150-160字符，用于搜索结果摘要）
│
├── 💰 交易
│   │
│   ├── 📋 订单列表
│   │   ├── 📊 KPI卡片
│   │   │   ├── 总订单数（计数）
│   │   │   ├── 总支付金额（金额，2位小数，千分位，单位：元）
│   │   │   └── 今日收入（金额，2位小数，单位：元）
│   │   ├── 🔍 筛选区
│   │   │   ├── 订单编号（文本输入，精确匹配）
│   │   │   ├── 用户ID（文本输入，精确匹配）
│   │   │   ├── 支付时间范围（日期选择器，开始日期+结束日期）
│   │   │   └── 订单状态（下拉：全部/已支付/待支付/已退款）
│   │   ├── 📄 表格列
│   │   │   ├── 订单编号
│   │   │   ├── 用户ID
│   │   │   ├── 购买智点（数字，千分位）
│   │   │   ├── 支付金额（金额，2位小数，千分位，单位：元）
│   │   │   ├── 支付方式（本地化显示：微信扫码）
│   │   │   ├── 支付时间（时间戳，格式：YYYY-MM-DD HH:mm:ss）
│   │   │   ├── 状态（已支付/待支付/已退款）
│   │   │   └── 操作（查看详情按钮）
│   │   └── ⚙️ 操作交互：查看详情（弹窗显示订单完整信息）
│   │
│   └── 📋 模型成本
│       ├── 📊 KPI卡片
│       │   ├── 模型总数（计数）
│       │   ├── 今日成本（金额，2位小数，单位：元，全部模型合计）
│       │   ├── 本月成本（金额，2位小数，单位：元，全部模型合计）
│       │   └── 各渠道成本分布（饼图或柱状图，按渠道分组）
│       ├── 🔍 筛选区
│       │   ├── 模型名称（文本输入，模糊匹配）
│       │   ├── 渠道（文本输入，如：volcengine、openai）
│       │   ├── 是否专属应用（下拉：全部/是/否）
│       │   └── 成本计算方式（下拉：全部/按token/按次/按档位）
│       ├── 📄 表格列
│       │   ├── 模型名称
│       │   ├── 渠道（如：volcengine）
│       │   ├── 成本计算方式（按token/按次/按档位）
│       │   ├── 是否专属应用（是/否）
│       │   ├── 上游单价（输入价格、补全价格、缓存价格，单位：元/千token或元/次）
│       │   ├── 今日消耗（金额，2位小数，单位：元）
│       │   ├── 今日成本（金额，2位小数，单位：元）
│       │   ├── 总成本（金额，2位小数，单位：元，历史累计）
│       │   ├── 最近同步时间（时间戳，格式：YYYY-MM-DD HH:mm:ss）
│       │   └── 操作（查看详情、同步数据）
│       └── ⚙️ 操作交互
│           ├── 批量同步（一键同步当前筛选结果下所有模型的上游成本）
│           ├── 查看详情（三栏布局：左侧导航/中栏模型列表/右栏成本明细）
│           └── 单个同步（详情面板内，同步单个模型的上游成本）
│
├── 📈 运营数据
│   │
│   ├── 📋 数据总览
│   │   ├── 日期范围选择器（默认近7天，可选：今日/近7天/近30天/自定义）
│   │   ├── 📊 KPI卡片
│   │   │   ├── 总任务数（计数，模型+图片+音视频合计）
│   │   │   ├── 成功数（计数）
│   │   │   ├── 失败退款数（计数）
│   │   │   ├── 成功率（百分比，=成功数/总任务数）
│   │   │   ├── 总消耗智点（数字，千分位）
│   │   │   └── 总成本（金额，2位小数，单位：元，全部渠道合计）
│   │   ├── 📄 分类明细表格
│   │   │   ├── 模型调用（任务数、成功数、失败退款数、消耗智点、成本、成本占比）
│   │   │   ├── 图片工具（任务数、成功数、失败退款数、消耗智点、成本、成本占比）
│   │   │   ├── 音视频工具（任务数、成功数、失败退款数、消耗智点、成本、成本占比）
│   │   │   └── 合计（任务数、成功数、失败退款数、消耗智点、成本、100%）
│   │   ├── 📊 奖励汇总卡片
│   │   │   ├── 累计发放智点（数字，千分位）
│   │   │   ├── 今日发放量（数字）
│   │   │   ├── 本周发放量（数字）
│   │   │   └── 累计奖励人数（计数）
│   │   └── ⚠️ 异常告警区
│   │       └── 失败率>10%的模型/工具列表（名称、失败率、失败数、跳转对应日志）
│   │
│   ├── 📋 模型调用日志
│   │   ├── 📊 KPI卡片
│   │   │   ├── 任务总数（计数）
│   │   │   ├── 成功数（计数）
│   │   │   ├── 失败后退款数（计数）
│   │   │   ├── 消耗智点（数字，千分位）
│   │   │   └── 成本总计（金额，2位小数，单位：元）
│   │   ├── 日期范围选择器（默认近7天）
│   │   ├── 🔍 筛选区
│   │   │   ├── 任务ID（文本输入，精确匹配）
│   │   │   ├── 对话名称（文本输入，模糊匹配）
│   │   │   ├── 模型名称（文本输入，模糊匹配）
│   │   │   ├── 用户ID（文本输入，精确匹配）
│   │   │   ├── 模型类型（下拉：全部/对话模型/其他）
│   │   │   └── 扣费状态（下拉：全部/已扣费/已退款）
│   │   └── 📄 表格列
│   │       ├── 任务ID
│   │       ├── 对话名称
│   │       ├── 使用模型
│   │       ├── 模型类型
│   │       ├── 消耗智点（数字，千分位）
│   │       ├── 成本费用（金额，2位小数，单位：元）
│   │       ├── 关联状态（成功/失败/进行中）
│   │       └── 执行时间（时间戳，格式：YYYY-MM-DD HH:mm:ss）
│   │
│   ├── 📋 图片工具调用日志
│   │   ├── 📊 KPI卡片
│   │   │   ├── 任务总数（计数）
│   │   │   ├── 成功数（计数）
│   │   │   ├── 失败后退款数（计数）
│   │   │   ├── 消耗智点（数字，千分位）
│   │   │   └── 成本总计（金额，2位小数，单位：元）
│   │   ├── 日期范围选择器（默认近7天）
│   │   ├── 🔍 筛选区
│   │   │   ├── 任务ID（文本输入，精确匹配）
│   │   │   ├── 工具名称（文本输入，模糊匹配）
│   │   │   ├── 场景名称（文本输入，模糊匹配）
│   │   │   ├── 用户ID（文本输入，精确匹配）
│   │   │   └── 扣费状态（下拉：全部/已扣费/已退款）
│   │   └── 📄 表格列
│   │       ├── 任务ID
│   │       ├── 工具名称
│   │       ├── 场景名称
│   │       └── 操作（成本与计费、调用详情）
│   │
│   ├── 📋 音视频工具调用日志
│   │   ├── 📊 KPI卡片
│   │   │   ├── 任务总数（计数）
│   │   │   ├── 成功数（计数）
│   │   │   ├── 失败后退款数（计数，与模型/图片日志统一命名）
│   │   │   ├── 消耗智点（数字，千分位）
│   │   │   └── 成本总计（金额，2位小数，单位：元）
│   │   ├── 日期范围选择器（默认近7天）
│   │   ├── 🔍 筛选区
│   │   │   ├── 任务ID（文本输入，精确匹配）
│   │   │   ├── 工具名称（文本输入，模糊匹配）
│   │   │   ├── 用户ID（文本输入，精确匹配）
│   │   │   ├── 扣费状态（下拉：全部/已扣费/已退款）
│   │   │   └── 计费模式（下拉：全部/固定扣费/倍率扣费）
│   │   └── 📄 表格列
│   │       ├── 任务ID
│   │       ├── 工具名称
│   │       ├── 消耗智点（数字，千分位）
│   │       ├── 成本费用（金额，2位小数，单位：元）
│   │       ├── 关联状态（成功/失败/进行中）
│   │       └── 执行时间（时间戳）
│   │
│   ├── 📋 奖励概览
│   │   └── 📊 KPI卡片
│   │       ├── 累计发放智点（数字，千分位）
│   │       ├── 今日发放量（数字）
│   │       ├── 本周发放量（数字）
│   │       └── 累计奖励人数（计数）
│   │
│   └── 📋 奖励记录
│       ├── 📊 KPI卡片
│       │   ├── 今日发放智点（数字）
│       │   ├── 本周发放智点（数字）
│       │   └── 累计发放智点（数字，千分位）
│       ├── 🔍 筛选区
│       │   ├── 发放时间范围（日期选择器，开始日期+结束日期）
│       │   └── 用户ID（文本输入，精确匹配）
│       └── 📄 表格列
│           ├── 用户ID
│           ├── 奖励智点（数字，千分位）
│           ├── 奖励类型（如：注册奖励、活动奖励）
│           ├── 发放时间（时间戳，格式：YYYY-MM-DD HH:mm:ss）
│           └── 备注（文本）
│
└── 👤 用户
    │
    └── 📋 用户列表
        ├── 📊 KPI卡片
        │   ├── 总用户数（计数）
        │   ├── 今日新增（计数）
        │   ├── 总智点余额（数字，千分位，全部用户合计）
        │   └── 总购买额（金额，2位小数，千分位，单位：元，全部用户合计）
        ├── 🔍 筛选区
        │   ├── 用户ID（文本输入，精确匹配）
        │   ├── 手机号（文本输入，精确匹配）
        │   └── 注册时间范围（日期选择器，开始日期+结束日期）
        ├── 📄 表格列
        │   ├── 用户ID
        │   ├── 绑定手机号
        │   ├── 剩余智点（数字，千分位）
        │   ├── 已消耗智点（数字，千分位）
        │   ├── 累计购买总额（金额，2位小数，千分位，单位：元）
        │   └── 操作（用户详情、赠送智点）
        ├── ⚙️ 操作交互
        │   ├── 查看详情（弹窗）
        │   └── 赠送智点（弹窗，输入正整数额度，大额二次确认）
        └── 📝 详情弹窗
            ├── 基本信息
            │   ├── 用户ID（只读）
            │   ├── 绑定手机号（只读）
            │   ├── 用户名（只读）
            │   ├── 昵称（只读）
            │   ├── 剩余智点（只读）
            │   ├── 已消耗智点（只读）
            │   ├── 累计购买智点（只读）
            │   ├── 累计赠送智点（只读）
            │   ├── 累计购买总额（只读）
            │   ├── 累计智点总额（只读）
            │   ├── 注册时间（只读）
            │   └── 最近登录时间（只读）
            ├── 购买明细（Tab页）
            │   └── 列：订单编号、支付金额、支付时间、支付方式（微信扫码）、购买智点
            │   └── 可跳转订单列表（带用户ID筛选）
            └── 智点变动明细（Tab页）
                └── 列：变动类型（充值/消耗/赠送/退款）、变动智点（±）、变动后余额、变动时间
