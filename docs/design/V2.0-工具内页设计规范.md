# V2.0 工具内页设计规范

> 基于语音转文字页面重构实战沉淀，作为后续所有工具内页的统一标准。
> 与 V1.3 规范的差异点已在各条目标注"V2 改动"。

---

## 1. 框架与布局

| 项 | 值 | 备注 |
|----|-----|------|
| 右侧内容最大宽度 | `max-w-[1440px] mx-auto px-[34px]` | 仅作用于内容区 |
| Header | 全宽 `px-[34px]`，不受 1440px 限宽 | V2 改动 |
| Header 高度 | `h-16`（64px） | |
| Header 背景 | `rgba(255,255,255,0.96)` + `backdrop-blur-[10px]` | 毛玻璃，V2 改动 |
| Header 下边框 | `rgb(237,240,248)` | V2 改动 |
| Header 与内容区间距 | `pt-9`（36px） | V2 改动 |
| 内容区间距 | `flex flex-col gap-6`（24px） | |
| 页面底色 | `#FFFFFF` | |
| 页面最小验收宽度 | `1400px` | |

---

## 2. 颜色令牌

### 2.1 文字层级

| 层级 | 色值 | 用途 |
|------|------|------|
| --ink | `#202434` | 仅用于 body 正文、输入框内文字 |
| --text | `#3f4558` | 所有标题（h1-h4）、卡片标题、工具名 | V2 改动 |
| --muted | `#8a91a6` | 描述文案、提示文字、FAQ 答案 |
| --label | `#a0a7b8` | 列表 dt 标签、表单 label | V2 新增 |
| --value | `#697185` | 列表 dd 信息值 | V2 新增 |
| --nav | `#596176` | 收藏/分享/下载等导航按钮文字 | V2 新增 |
| --weak | `#9ca2b5` | 删除等危险操作默认色 | V2 新增 |
| --faint | `#9ca3b8` / `#b7becf` | 费用提示、极弱信息 |

**核心原则**：标题和区域标题统一用 `--text (#3f4558)`，不再用 `--ink (#202434)`。

### 2.2 背景

| 层级 | 色值 | 用途 |
|------|------|------|
| --canvas | `#f8faff` | 上传区、转写/总结内容区、音频播放器 |
| --card | `#fdfdff` | 使用指南卡片背景 | V2 改动 |
| --card-white | `#ffffff` | 生成设置、生成结果、历史列表 |
| --card-glass | `rgba(255,255,255,0.96)` | 生成结果外层卡片 | V2 新增 |
| --hover-row | `#fbfcff` | 历史列表行 hover | V2 新增 |

### 2.3 边框

| 层级 | 色值 | 用途 |
|------|------|------|
| --border-card | `#f0f2f8` | 大卡片外边框 |
| --border-inner | `#e7ebf5` | 内部分隔线、输入框、设置行 |
| --border-nav | `#e2e6f3` | 下载按钮边框 | V2 新增 |
| --border-guide | `#e0e5ff` | 使用指南卡片边框 | V2 新增 |
| --header-border | `rgb(237,240,248)` | Header 底部 | V2 新增 |

### 2.4 品牌色

| 层级 | 色值 | 用途 |
|------|------|------|
| --brand | `#4f55ec` | 主按钮、播放按钮、图标、Select focus |
| --brand-bg | `bg-[#4f55ec]/[0.08]` | 图标容器背景 |
| --brand-bg-hover | `bg-[#4f55ec]/[0.06]` | Select hover、导航按钮 hover |
| --brand-glow | `rgba(87,92,233,0.22)` | 生成按钮阴影 |
| --brand-glow-sm | `rgba(87,92,233,0.14)` | 保存/开始总结按钮阴影 |
| --brand-glow-cover | `rgba(87,92,233,0.08)` | 封面图阴影 |

---

## 3. 字体规范

### 3.1 标题层级

| 层级 | 字号 | 字重 | 颜色 | 场景 |
|------|------|------|------|------|
| Hero h1 | `20px` | `500` | `#3f4558` | 场景介绍页大标题 |
| 区域 h2 | `18px` | `500` | `#3f4558` | 使用流程/功能特性/适用场景/FAQ |
| 卡片 h3 | `18px` | `500` | `#3f4558` | 生成结果/生成历史/参数设置/生成设置 |
| 内容 h4 | `14px` | `500` | `#3f4558` | 转写内容/智能总结/视频预览/字幕文本 | V2 改动：颜色统一为 #3f4558 |
| 指南标题 | `17px` | `400` | `#3f4558` | 使用指南（`<span>` 内联） |

**原则**：一个区域最多一个标题，上传区/输入区不单独设标题，直接展示上传控件即可。所有标题统一使用 `#3f4558`，禁止使用 `text-foreground`。

### 3.2 正文层级

| 层级 | 字号 | 颜色 | 场景 |
|------|------|------|------|
| 描述正文 | `16px` | `text-muted-foreground` | 使用指南描述 |
| 描述正文（场景介绍） | `13px` | `#8a91a6` | 功能特性/场景卡片/FAQ |
| 设置 label | `14px` | `#3f4558` | 生成设置行 |
| 设置副文本 | `11px` | `text-muted-foreground` | Switch 描述 |
| 上传主文字 | `17px` | `#3f4558` | 不加粗 |
| 格式说明 | `13px` | `text-muted-foreground` | 支持格式/大小限制 |
| 免责声明 | `12px` | `text-muted-foreground/70` | 合规提示 |
| 列表标签 | `12px` | `#a0a7b8` | 文件名称/转写内容/创建时间 |
| 列表值 | `14px` | `#697185` | 实际文件名/内容/时间 |
| 历史提示 | `14px` | `text-muted-foreground` | "保留 3 天..." |
| 费用文字 | `11px` | `#b7becf` | 生成设置底部费用 |
| 指南费用 | `14px` | `#9ca3b8` | 使用指南底部费用（`<small>`） |

### 3.3 行高

| 场景 | 值 |
|------|-----|
| 使用指南描述 | `leading-[1.7]` |
| 其他描述 | `leading-relaxed`（1.625） |
| 列表行 | `py-3`（12px 上下） |

---

## 4. 圆角规范

| 层级 | 值 | 用途 |
|------|-----|------|
| 大面板 | `18px` | 生成结果、生成历史外层 | V2 改动 |
| 中卡片 | `16px` | 使用指南、功能特性、适用场景 | V2 改动 |
| 小卡片 | `14px` | 生成设置内部面板、历史列表容器、FAQ 项 |
| 生成设置外层 | `16px` | |
| 上传区/内容区 | `12px` | 上传区虚线框、转写/总结 textarea |
| 封面图 | `18px` | |
| 按钮（主要） | `10px` | 生成/保存/开始总结 |
| 按钮（辅助） | `7px` | 编辑/复制/导出 ghost |
| 导航按钮 | `8px` | 收藏/分享 |
| Tabs 外框 | `10px` | |
| Tabs 内按钮 | `7px` | |
| Select | `11px` | |
| 图标容器 | `10px` | |

---

## 5. 阴影规范

| 层级 | 值 | 用途 |
|------|-----|------|
| 卡片默认 | `0 8px 22px rgba(38,44,72,0.028)` | 所有带阴影的卡片 |
| 生成按钮 | `0px 12px 28px rgba(87,92,233,0.22)` | 立即生成 |
| 操作按钮 | `0px 8px 16px rgba(87,92,233,0.14)` | 保存/开始总结 |
| 封面图 | `0px 16px 42px rgba(87,92,233,0.08)` | |
| hover 加深 | `0 4px 16px rgba(38,44,72,0.06)` | 功能特性/场景卡片 hover |
| 使用指南 | 无 | V2 去掉阴影 |

---

## 6. 按钮层级体系

### 6.1 层级定义

| 层级 | 用途 | 高度 | 字号 | 字重 | 圆角 | 背景 | 文字色 | 阴影 |
|------|------|------|------|------|------|------|--------|------|
| **主要** | 立即生成/保存/开始总结 | `46px` | `15px` | `500` | `10px` | `#4f55ec` | `#fff` | 品牌色 |
| **辅助 ghost** | 编辑/复制/导出/重新生成 | `34px` | `14px` | `400` | `7px` | 透明 | `text-muted-foreground` → hover `text-foreground` | 无 |
| **导航 outline** | 收藏/分享 | `36px` | `13px` | `400` | `8px` | `#fff` | `#596176` | 无 |
| **操作 outline** | 下载 | `34px` | `13px` | `400` | `8px` | `#fff` | `#596176` | 无 |
| **危险 ghost** | 删除 | `34px` | `13px` | `400` | — | 透明 | `#9ca2b5` → hover `destructive` | 无 |
| **取消 outline** | 取消（编辑态） | `46px` | `15px` | `400` | `10px` | 透明 | 默认 | 无 |
| **发送 icon** | 总结输入框发送 | `32px` | — | — | `6px` | `#4f55ec` | `#fff` | 无 |

### 6.2 导航按钮 hover

仅背景变色：`hover:bg-[#4f55ec]/[0.06]`。文字、图标、边框颜色不变。

### 6.3 下载按钮 hover

`hover:bg-[#f3f5ff] hover:border-[#dfe3ff]`。文字颜色不变。

---

## 7. 标题渐变色

### 7.1 作用域

渐变**仅覆盖标题行**，不延伸到内容区。实现方式：标题行用独立 `<div>` 包裹，内容区用另一个 `<div>`。

### 7.2 样式

```
background: linear-gradient(180deg, #f8faff 0%, #ffffff 100%)
padding: px-6 pb-2（或 pt-6 pb-2）
```

### 7.3 应用卡片

- 生成结果（loading 态 + 结果态）
- 生成历史

### 7.4 不应用

- 使用指南（无渐变）
- 生成设置（无渐变）
- 场景介绍（无渐变）

---

## 8. 图标规范

### 8.1 图标库

统一使用 `@phosphor-icons/react` React 组件。不使用 `lucide-react`，不使用 `<i class="ph">` 字体图标（未全局引入 CSS）。

### 8.2 图标尺寸

| 场景 | 尺寸 |
|------|------|
| Header 工具图标 | `24px`（`h-6 w-6`） |
| Header 导航按钮图标 | `17px` |
| 场景介绍图标 | `22px` |
| 场景介绍步骤编号 | `12px`（`text-xs`） |
| 操作按钮图标 | `14px`（`h-3.5 w-3.5`） |
| 列表行图标 | `16px`（`h-4 w-4`） |
| 上传图标 | `60px`（style） |
| 文件信息卡图标 | `28px`（`h-7 w-7`） |
| 文件类型小标签图标 | `12px`（`h-3 w-3`） |
| FAQ 箭头 | `20px`（`h-5 w-5`） |
| 播放/暂停 | `12px`（`h-3 w-3`） |

### 8.3 图标颜色

| 场景 | 颜色 |
|------|------|
| Header 工具图标 | `#4f55ec` |
| 上传图标 | `#4f55ec` |
| 操作按钮图标 | `text-muted-foreground` → hover `text-foreground` |
| 列表行图标 | `text-sky-500` |
| 设置行图标 | 无 |
| 场景介绍步骤图标 | `#4f55ec` |
| 功能特性图标 | `#4f55ec` |
| 适用场景图标 | 独立语义色（indigo/teal/fuchsia/amber/rose/slate） |
| FAQ 箭头 | `#8a91a6` |
| 播放按钮 | 白色（品牌色底） |

---

## 9. 交互状态

### 9.1 列表行

| 状态 | 样式 |
|------|------|
| 默认 | `bg-white` |
| Hover | `bg-[#fbfcff]` |
| 最后一行 | 无底边框 `[&>*:last-child]:border-b-0` |

### 9.2 Select

| 状态 | 样式 |
|------|------|
| 默认 | `border-[#e7ebf5] bg-white shadow-none` |
| Hover | `bg-[#4f55ec]/[0.04]` |
| Focus | `focus:ring-2 focus:ring-[#4f55ec]/20 focus:border-[#4f55ec]/40` |

**原则**：下拉组件本体无阴影（`shadow-none`），只有弹出的气泡面板（SelectContent / PopoverContent）才有阴影。

### 9.3 上传区

| 状态 | 样式 |
|------|------|
| 默认 | `border-2 border-dashed border-[#e7ebf5] bg-[#f8faff] rounded-[12px] py-10 px-8` |
| Hover | `hover:border-primary/30` |
| 拖拽中 | `border-primary bg-primary/[0.04]` |
| 上传后 | `border border-[#e7ebf5] bg-[#f8faff]`，hover 遮罩 `bg-black/40` |

**空白态元素**（flex-col + gap-3 + items-center + justify-center）：

| 元素 | 样式 |
|------|------|
| 图标 | `PlusCircle`，`fontSize: 60px`，`text-[#4f55ec]` |
| 主文字 | `text-[17px] text-[#3f4558]`（不加粗） |
| 格式提示 | `flex flex-col items-center gap-1 text-[13px] text-muted-foreground`（两行） |
| 免责声明 | `flex gap-1.5 text-[12px] text-muted-foreground/70 mt-3` |
| 触发方式 | `div` + `onClick` + `fileInputRef`（不用 `<label>`） |

---

## 10. 间距规范

| 场景 | 值 |
|------|-----|
| Header 与内容区 | `pt-9`（36px） |
| 卡片之间 | `gap-6`（24px） |
| 卡片内 padding | `p-6`（24px） |
| 设置行 | `px-4 py-3.5` |
| 标题渐变区与内容区 | `pb-2` + `pt-4`（标题行紧凑，内容区正常） |
| 操作按钮行间距 | `gap-1.5` |
| 警告文字与上方 | `mt-[7px]` |
| 免责声明与上方 | `mt-3` |
| 费用与按钮 | `mt-3` |
| 上传图标与文字 | `gap-3`（flex-col gap） |

---

## 11. 弹窗规范

- 确认弹窗：标题 16px 居中，正文 12px 居中，按钮等宽等高，无 shadow，点击空白不关闭
- 成功/失败提示：toast，不弹窗确认
- 删除操作：必须先 AlertDialog 确认

---

## 12. 场景介绍页特殊规范

### 12.1 区域间距

用 `mb-10`（40px）分隔各 section，不用 `border-t`。

### 12.2 使用流程

- 编号圆圈：`36px`（`w-9 h-9`）
- 图标容器：`36px`（`w-9 h-9`），`rounded-[10px]`
- 描述字号：`13px`

### 12.3 功能特性

- 卡片：`rounded-[16px]` 无边框，hover 阴影加深
- 图标容器：`40px`（`w-10 h-10`），`rounded-[10px]`
- 图标：`22px`

### 12.4 适用场景

- 卡片：`rounded-[16px]` 渐变背景 + `border border-[#e7ebf5]`
- 图标：独立语义色 + `bg-white/70` 容器

### 12.5 FAQ

- 项：`rounded-[14px]` 无边框
- 按钮：`py-[14px]`
- 答案：`13px`

---

*生成时间：2026-07-09*
*基于：agent-speech-to-text-experience.tsx + agent-speech-to-text-intro.tsx 最终代码*