# 图片模型输入区完整实现

## 核心功能完成

已成功为图片模型添加完整的输入区功能，保持与聊天模型相同的底部固定结构。

### 1. 参数工具栏

实现了完整的图片参数选择器：

**图片比例**：
- 自适应（默认）
- 1:1、3:2、2:3、16:9、9:16

**图片数量**：
- 1张（默认）
- 2张
- 4张

**图片质量**：
- 自适应（默认）
- 高、中、低

**提示词优化**：
- 切换按钮（开启/关闭）
- 默认开启状态
- 按钮状态清晰可视

### 2. 参考图上传

支持完整的参考图上传功能：
- 支持 JPG、PNG、WebP 格式
- 多图上传（最多5张）
- 缩略图预览显示
- 删除按钮（X图标）
- 上传限制提示（Tooltip）

### 3. 统一的底部固定输入区

保持与聊天模型完全相同的结构：
- 多行文本输入框
- Shift+Enter 换行支持
- 参数工具栏在输入框上方
- 发送按钮自适应启用状态
- 附件上传按钮（区分聊天/图片模式）

### 4. 完整的消息流工作

**用户消息生成**：
- 提示词捕获
- 参数保存（ratio、count、quality、optimizePrompt）
- 参考图列表
- 时间戳记录

**AI 图片生成回复**：
- 自动生成图片 URL（基于参数）
- 根据数量自适应网格：
  - 1张：单列
  - 2张：2列网格
  - 4张：2x2网格
- 添加时间戳
- 自动追加到消息流

### 5. 参数与回复的关联

完整的参数流转：
```
用户设置参数 → 发送消息 → 参数传递 → 生成对应数量的图片 → 显示在网格中
```

**验证结果**：
- 参数改为 4张时，AI 生成 4 张图片
- 参数显示在用户消息中
- 图片网格自适应布局

### 6. 交互体验

所有交互流畅：
- 参数选择器即时响应
- 参考图上传预览快速
- 消息发送后立即清空输入
- 参数状态保持直到修改

## 技术实现

**InputArea 组件更新**：
- 新增 `ImageParams` 接口
- 图片参数状态管理
- 参考图本地存储
- 条件渲染参数栏（根据 model.type）

**Workspace 组件更新**：
- 增强 `handleSendMessage` 处理图片消息
- 生成 `ImageMessage` 类型消息
- 模拟 AI 图片生成（返回 Unsplash 图片 URL）
- 根据参数生成对应数量的图片

**消息流集成**：
- ChatMessages 组件已支持图片消息渲染
- 参数标签、参考图、图片网格都在流中显示

## 验证清单

✓ 参数工具栏显示所有选项
✓ 参数下拉选择功能正常
✓ 参数改变实时更新显示
✓ 参考图上传与预览
✓ 消息发送生成用户消息
✓ AI 生成回复消息
✓ 图片数量与参数关联
✓ 图片网格自适应布局
✓ 时间戳正确记录
✓ 底部输入区固定不滚动
✓ 多行输入支持
✓ Shift+Enter 换行功能
✓ 参数在消息中显示为 Badge

## 后续优化

- 真实图片生成 API 集成
- 参考图实际上传到服务器
- 更多高级参数选项
- 图片生成进度显示
- 图片结果快速操作（下载、重用、收藏）
